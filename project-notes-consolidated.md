# Show Notice (Lowcountry Show Tracker) — Project Notes

Consolidated reference for anyone (human or automated session) picking up work on this repo. Merges the GitHub `main` version (last updated 2026-09-10, through PR #24) with the Claude-project research/operations notes (last updated 2026-09-09), plus everything from a long 2026-09-11 session: four same-day 2026-09-10 sessions carried over from the prior update (Riverfront Park festival entries + two standing policy decisions; a frontend UX pass, PRs #10–#17; a header/nav redesign, PR #19; redesign follow-ups, PRs #21–#24) — and then, on 2026-09-11 itself: the **Show Notice rebrand and domain migration** off `gigalertchs.com` onto `CHS.ShowNotice.com` (PRs #26–#31), a further round of frontend/UX fixes (PRs #32–#39), a full **admin analytics panel build-out** — subscriber/account/digest-engagement/favorited-artist&venue/starred-show/suggestion stats, each with week/month/year/all rolling-window columns, plus Cloudflare zone traffic & country data (PRs #40–#49), Workers Logs + Traces observability (PR #50), and a full-project debug/security/stability audit with 6 fixes (PR #51). Then, on 2026-09-12: 8 new venues onboarded plus source-vetting research (PRs #52–#65, see "Show data collection" and its research-log subsections below), an **account/subscribe model + Contact Us overhaul** — explicit subscribe/unsubscribe semantics tied to the sign-in checkbox, a combined About & Contact panel with a public Contact Us form replacing a visible admin email address, and cron schedule changes (PRs #66–#68) — and finally a **two-round nav/layout redesign** — pill reordering, the venue directory moved to the page bottom, sign-in relocated into My Shows, a multi-select venue filter on All Shows, and per-venue accent colors on venue pages (PRs #69–#70). Replace all prior versions with this file.

## What this is

A live-music show tracker for the Charleston, SC area, branded **Show Notice**.
- **Site:** [chs.shownotice.com](https://chs.shownotice.com/) — static frontend, served via GitHub Pages.
- **API:** [api.chs.shownotice.com](https://api.chs.shownotice.com/) — Cloudflare Worker backend.
- **Repo:** [github.com/ShowNotice/Show-Tracker-CHS](https://github.com/ShowNotice/Show-Tracker-CHS).
- **Admin panel:** `admin.html`, served from the same site, owner-only (Turnstile + magic-link + `OWNER_EMAIL` check).

Static frontend + a Cloudflare Worker backend for accounts, subscriptions, a weekly digest email, and (as of 2026-09-11) a full admin analytics dashboard. Show data is refreshed by a separate automated research routine that pushes `shows.json` updates via PR.

**Former identity, retired 2026-09-11:** this project was previously "GigAlert CHS," served from `gigalertchs.com` (repo `GigAlertCHS/Show-Tracker`) and the Worker's `*.workers.dev` fallback URL. See "Domain migration" below — `gigalertchs.com` now 301-redirects to `chs.shownotice.com` (confirmed working on both desktop and mobile) rather than being decommissioned outright, so old links/bookmarks keep working.

## Files

| File | Role |
|---|---|
| `index.html` | The public site. Static HTML/CSS/vanilla JS, no build step. Fetches `shows.json` client-side and renders it. Served via GitHub Pages (custom domain via `CNAME`). |
| `worker.js` | Cloudflare Worker backend: passwordless sign-in, My Shows/Favorite Artists storage, digest email, admin stats (incl. Cloudflare zone analytics), venue suggestions. Deployed via `.github/workflows/deploy-worker.yml`. |
| `admin.html` | Owner-only stats dashboard. Talks to the Worker's `/api/admin/stats`; calls it at `api.chs.shownotice.com` directly (not the `*.workers.dev` fallback — see Domain migration). Also static, no build step. |
| `shows.json` | The show data itself: `{ dataUpdatedAt, venues, shows }`. Updated by the weekly automated research routine, not hand-edited. |
| `wrangler.toml` | Worker deploy config: KV binding, Secrets Store bindings, cron trigger, custom domain route, observability, plain vars. Source of truth for `wrangler deploy` — see the warning comment at the top of the file about what gets silently stripped if this file is wrong/missing. |
| `CNAME` | GitHub Pages custom domain — now `chs.shownotice.com` (moved 2026-09-11, was `gigalertchs.com`). |
| `.github/workflows/deploy-worker.yml` | Auto-deploys `worker.js` to Cloudflare on every push to `main` that touches `worker.js` or `wrangler.toml`. Runs `node --check` plus a duplicate-top-level-function-declaration scan as a sanity check before deploying. No test suite, no required review — see "Deploy process" below. |

`index.html`/`admin.html`/`shows.json` need no separate deploy step — GitHub Pages serves them straight from the repo.

## Data model

### `shows.json`

```
{
  "dataUpdatedAt": "YYYY-MM-DD",
  "venues": { "<CODE>": { "name", "site", "type": "venue"|"festival", "color", "address"? } },
  "shows": [ { "d", "b", "v", "sh", "dr", "o"?, "s"?, "p"?, "dop"?, "u"?, "e"?, "added"?, "flag"?, "soldOut"?, "lowTix"? } ]
}
```

Show fields:
- `d` — date (`YYYY-MM-DD`). `e` — optional end date for multi-day events.
- `b` — band/event name. `o` — opener(s), comma-separated if a list. A single opener now renders inline on one line ("Band w/ Opener"), wrapping naturally — this replaced an earlier format where the opener sat on its own line below the band name. Multiple comma-separated openers still render as separate pill chips below the band name (that part is unchanged).
- `v` — venue code, must be a key in `venues`. Known codes as of this writing: `PH` (Pour House), `REF` (The Refinery), `CMH` (Music Hall), `WJ` (Windjammer), `C1S` (Credit One Stadium), `NCC` (N. Charleston Coliseum), `MF` (Music Farm), `FD` (Firefly Distillery), `GC` (Gaillard Center), `NRB` (New Realm Brewing), `TR` (Tin Roof), `RA` (The Royal American), plus festival/park entries `CJF` (Charleston Jazz Festival) and `RFP` (Riverfront Park — hosts both Riverfront Revival and Low Tide Festival; neither gets its own venue code, since both are festivals held at this venue rather than independent venues). A standalone `RFR` (Riverfront Revival) venue code existed briefly but was removed 2026-09-10 — Riverfront Revival isn't its own venue, it's an event at Riverfront Park.
- `sh`/`dr` — show/doors time as free text (`"7:00 PM"`, `"—"`, `"see ticket link"`, `"TBA"`).
- `s` — stage name, when a venue has multiple stages.
- `p` — advance price (number, `0` = free). Always the advance price when a show has one — stored and shown by default up through the day before the show. Many venues list both an advance and day-of price (e.g. "$25 advance / $30 day of show," or "$13 – $15") — always store the lower/advance figure in `p`, never the day-of figure or an average.
  - **Tiered/multi-day festival pricing fallback (decided 2026-09-10):** some festivals (multi-day passes, GA/GA+/VIP/Stage Access tiers, prices that visibly increase over time via presale windows) don't reduce to one honest "advance price" number — picking any single tier would misrepresent the others. When pricing is this fragmented, omit `p` entirely and rely on `u` (ticket link) alone rather than forcing a single figure. Reserve this for genuine multi-tier/multi-day spreads, not as a shortcut when a single advance price is knowable but just requires an extra fetch.
- `dop` — optional day-of price, only used once the show's actual date has arrived (compares the visitor's local date to `d`). Both the site (`priceOrTicketsHTML()` in `index.html`) and the digest email (`buildDigestEmailHTML` in `worker.js`) apply this rule identically — if one changes, the other needs the same change.
- `u` — ticket URL. Validated as `http(s)://` before ever being put in an `href`, both client-side (`safeUrl()` in `index.html`) and server-side (`worker.js`), since `shows.json` is written by an automated scraping agent and its URLs aren't implicitly trusted.
- `added` — date this show was first added to the data. Drives the site's "Added This Week" section and the digest email's equivalent (cutoff: added within the last 7–10 days depending on which surface).
- `flag` — a one-line human-readable note for genuinely ambiguous cases (e.g. hybrid non-music/music events — see exclusion rules below). Shown as-is in the data, but **not rendered anywhere on the live site** (confirmed by checking the code — the CSS class exists but nothing ever creates an element with it); it's an internal/editorial note only.
- `soldOut` / `lowTix` — booleans, independent of `flag`, and the only fields that actually drive visible ticket-status UI. `soldOut: true` is set when the venue's own page explicitly shows "Sold Out" — the site shows a muted-red "Sold Out" label (`--sold-out: #c96a6a`) in place of the price/Tickets link, not a link, since there's nothing to click through to. `lowTix: true` is set when the venue shows a low-inventory status (e.g. Music Farm's "LOW TIX") — tickets are still purchasable, so the site shows a gold "Low Tix" label that's still a working link. Both must be respected everywhere a price/ticket link renders — `index.html`'s `priceOrTicketsHTML()` and `worker.js`'s `buildDigestEmailHTML`/`showRowHtml` price block are the two implementations and must stay in sync (this was out of sync until the 2026-09-10 fix — see Incident history).
- `"g": "classical"` should not be used at all going forward (classical/orchestral is excluded entirely — see exclusion rules). As of the last full audit, zero shows in the dataset carry this tag. An earlier design (hide-by-default with a per-venue reveal toggle) was removed from the code entirely, not just left dormant.

`showId(s)` — `[v, d, b].join('|').toLowerCase().replace(/\s+/g, '_')` — must be kept identical between `index.html` and `worker.js`. It's the join key between a subscriber's favorited/starred shows (client-side `MY_SHOWS`, server-side `myShows` in KV) and the actual show list. If one side's version of this function ever changes, the other needs the same change or personalization quietly breaks.

### Cloudflare KV (`SHOW_TRACKER_KV`)

| Key pattern | Value | Notes |
|---|---|---|
| `token:<uuid>` | `{ email }` | Magic-link sign-in token. 15 min TTL, one-time use (deleted on verify). |
| `session:<uuid>` | `{ email }` | Session token, sent as `Authorization: Bearer`. 30-day TTL. |
| `subscriber:<email>` | `{ subscribedAt, unsubscribeToken }` | Presence = actively subscribed to the digest. |
| `unsubtoken:<uuid>` | `<email>` (plain string) | Reverse lookup from a subscriber's unsubscribe token to their email. Reused as the auth credential for `/api/star-show` too — same trust level either way, since whoever holds it can already unsubscribe the person. |
| `unsubscribed:<email>` | `{ unsubscribedAt }` | Permanent marker so a later sign-in doesn't silently re-subscribe someone who opted out. No TTL. |
| `user:<email>` | `{ myShows: [showId...], favorites: [artistName...], createdAt?, myShowsAddedAt?, favoritesAddedAt? }` | Both arrays length/count-capped (`MAX_SHOW_ID_LENGTH` = 150, favorites item cap 100 chars, both arrays capped to `MAX_MY_SHOWS` = 300) since this is written from unauthenticated-by-anything-but-a-token paths (`/api/star-show`) as well as the authenticated `/api/user/data`. The three `*At` timestamp fields were added 2026-09-11 to drive the admin panel's week/month/year/all rolling-window stats; they self-heal via a one-time backfill on first read for any record created before this change, rather than needing a migration script — `myShows`/`favorites` themselves stay plain string arrays throughout (a deliberate side-channel design, not a schema change) so none of the extensive existing client-side code touching those two fields needed to change. |
| `suggestion:<uuid>` | `{ submitterEmail, venueName, notes, submittedAt }` | From "Suggest a Venue." |
| `contact:<uuid>` | `{ email, message, submittedAt }` | From the Contact Us form (added 2026-09-12). `email` is optional — a visitor can submit with none. |
| `ratelimit:<email>` | `'1'`, 60s TTL | One sign-in link request per email per minute. |
| `ratelimit-ip:<ip>` | request count string, 1hr TTL | Best-effort per-IP cap (10/hr) on sign-in requests — not atomic; KV has no atomic increment, so a burst of simultaneous requests can overshoot the cap. A hard guarantee would need Durable Objects. Treated as a best-effort volume brake, with the per-email limit and Turnstile as the tighter controls. |
| `suggest-ratelimit:<email>` | `'1'`, 60s TTL | One venue suggestion per person per minute. |
| `contact-ratelimit:<ip>` | request count string, TTL matches the other rate limits | Per-IP cap on Contact Us submissions (added 2026-09-12) — same best-effort/non-atomic caveat as `ratelimit-ip` above; Turnstile is the tighter control in front of it. |

`listAllKeys(env, prefix)` pages through `KV.list()`'s cursor — added 2026-09-10 after noticing every list-all call site was using a single unpaginated `list()`, which silently truncates past 1000 keys. Use it for any new "every key with this prefix" need instead of calling `KV.list()` directly.

## Worker API routes

| Route | Method | Auth |
|---|---|---|
| `/api/auth/request-link` | POST | none (rate-limited + Turnstile if configured) |
| `/api/auth/verify` | GET | magic-link token (query) |
| `/api/user/data` | GET/POST | session Bearer token |
| `/api/unsubscribe` | GET | unsubscribe token (query) + `confirm=1` |
| `/api/star-show` | GET | unsubscribe token (query, reused) + `confirm=1` |
| `/api/subscribe` | POST | session Bearer token |
| `/api/digest-preview` | GET | session Bearer token (preview your own digest) |
| `/api/test-send-digest-now` | GET | session Bearer token, owner-only |
| `/api/report-conflicts` | POST | `COWORK_API_SECRET` shared secret (Bearer), timing-safe compare |
| `/api/suggest-venue` | POST | session Bearer token |
| `/api/contact` | POST | none (rate-limited + Turnstile) — added 2026-09-12 |
| `/api/admin/stats` | GET | session Bearer token, owner-only |

`/api/unsubscribe` and `/api/star-show` both require an explicit `confirm=1` click past a summary page rather than acting on a bare GET — added so a mail-security scanner or link-prefetcher auto-fetching a link straight out of the email body can't silently unsubscribe someone or star a show on their behalf.

## Secrets & environment variables

Set in Cloudflare (dashboard or Secrets Store), **never** committed:
- `RESEND_API_KEY` — Resend email API key. Until set, `/api/auth/request-link` runs in "test mode" and returns the magic link directly in the JSON response instead of emailing it.
- `TURNSTILE_SECRET_KEY` — Cloudflare Turnstile bot-protection secret. **Fails open** (skips verification) if absent — deliberate, so frontend/backend can be deployed slightly out of order without locking out sign-in, but means bot protection is silently off until this is actually set.
- `COWORK_API_SECRET` — shared secret for the automated research routine's `/api/report-conflicts` calls.
- `RESEND_WEBHOOK_SECRET` (added 2026-09-11) — verifies Resend's delivery/open/click webhook callbacks using the Svix signing scheme (HMAC-SHA256 over `{svix-id}.{svix-timestamp}.{body}`, compared timing-safe). Feeds the admin panel's digest-engagement stats.
- `CLOUDFLARE_ANALYTICS_TOKEN` (added 2026-09-11) — a Cloudflare API token scoped to Analytics/GraphQL read access, used by `fetchCloudflareZoneAnalytics()` to pull traffic/threat/cache/country data from Cloudflare's GraphQL Analytics API (`httpRequests1dGroups`) into the admin panel. Needs Secrets Store permissions specifically, not just an Analytics-read template — see "Deploy process" below for the same caveat on the CI deploy token.

Plain vars (in `wrangler.toml`, safe to commit): `RESEND_FROM_ADDRESS`, `SITE_URL`, `WORKER_BASE_URL`, `OWNER_EMAIL`, `CLOUDFLARE_ZONE_ID` (added 2026-09-11 — not secret, just identifies which zone `fetchCloudflareZoneAnalytics` queries).

**Secrets Store secret names do not match their binding names** (cost real debugging time): bindings are uppercase (what `worker.js` reads off `env`), but the actual secret names in the store are `RESEND_API_KEY` (uppercase), `turnstile-secret-key` (lowercase, hyphenated), `cowork-api-secret` (lowercase, hyphenated), `resend-webhook-secret` (lowercase, hyphenated), `cloudflare-analytics-token` (lowercase, hyphenated). Store ID is `30c772729ee04e95b2d1ee57b4be87fd`. KV namespace is `SHOW_TRACKER_KV`, id `d6ce7eeb579342ddaab94ae9560ddfc2`.

`wrangler.toml` is the deploy source of truth — anything a deploy needs that isn't declared there gets silently dropped. This has happened twice: once stripping the KV binding (broke sign-in entirely) and once stripping the Secrets Store bindings (broke magic-link email and silently disabled Turnstile). Read the comments at the top of that file before touching it.

## Deploy process

`.github/workflows/deploy-worker.yml` runs on every push to `main` touching `worker.js` or `wrangler.toml` (or manual `workflow_dispatch`). Steps: sanity-check → `wrangler deploy --dry-run` → `wrangler deploy`. **No required review, no test suite** — a push to `main` that touches those files goes live automatically. No branch-protection/required-review rule exists on `main` as of this writing (a GitHub repo setting, not fixable via a commit) — worth turning on if that's not already in place.

The sanity-check step (added 2026-09-10) runs `node --check worker.js` plus a script that fails the build if any top-level `function`/`async function` name is declared more than once — added specifically because `wrangler deploy --dry-run` did **not** catch the corruption incident below (the duplicated file was still syntactically valid, just semantically broken).

Other deploy notes:
- `worker.js` is tracked in git and deploys automatically via this workflow — manual pasting into Cloudflare's Edit Code UI still works as a fallback but is no longer required.
- `cloudflare/wrangler-action` swallows Wrangler's own output and reports only "exit code 1," which made two separate failures impossible to diagnose. The workflow now calls `npx wrangler@4 deploy` directly as a plain `run:` step, plus a `--dry-run` validation step first — keep it that way, since the visible error output is what finally solved those failures.
- The `CLOUDFLARE_API_TOKEN` used by GitHub Actions needs Secrets Store permissions, not just Workers Scripts. A token made from the "Edit Cloudflare Workers" template alone fails with error 10021 on any deploy touching Secrets Store bindings.
- The Cowork automation path (for the weekly data-update routine) was abandoned — its GitHub connector fails OAuth against the official GitHub MCP server. Weekly data updates instead run through a **Claude Code Routine**: cloud-hosted, scheduled, pushes to a `claude/`-prefixed branch and opens a PR for review rather than committing straight to main.
- `test_worker.js` (a 30-test suite referenced in older notes) is not in the repo and has never been located. **No automated test suite exists** — every deploy this session was validated manually (`node --check`, the duplicate-declaration scan, and the `code-review` skill) rather than by a test run. Worth building a small smoke-test suite for the Worker's request handlers eventually; flagged as an open suggestion, not yet actioned.
- **Workers Logs + Traces observability** (added 2026-09-11, `[observability.logs]`/`[observability.traces]` in `wrangler.toml`, `head_sampling_rate = 1`, `persist = true` on both) is declared in config for the same reason every other binding is — turning it on in the dashboard alone wouldn't survive the next deploy. Optional; nothing in the Worker depends on it to function.

## Incident history

### 2026-09-09: worker.js corruption (PRs #3, #4)

Two PRs, both titled *"Update print statement from 'Hello' to 'Goodbye'"* with no description, were merged directly to `main`. Despite the placeholder titles, they replaced the hand-written `worker.js` source with esbuild-bundled output, committed twice (`__name()`/`__defProp()` bundler artifacts, comments stripped, every function declared twice back-to-back). Because JS lets a later top-level `function` declaration silently shadow an earlier one, only the *second* copy of each function actually ran — this included a digest-email header-border fix that looked shipped but was actually dead code the whole time.

No malicious intent was found in the diff (no new external domains, no credential exfiltration, no eval/backdoor patterns) — this reads as an accidental bundle-output-committed-as-source mistake, twice, under a badly mismatched placeholder commit message, not a compromise. But it went straight to `main` and would have auto-deployed live with zero review gate, which is the actual systemic issue (see "Deploy process" — the sanity-check step exists because of this).

**Fixed in PR #5** (merged 2026-09-10): rebuilt `worker.js` from the last known-good commit (`806090f`) and manually ported forward the legitimate feature work that had been buried in the duplicate (the `/api/star-show` My Shows email feature).

If a commit/PR title doesn't match its diff size, or an empty PR body sits on a substantial change, treat it the way this one should have been treated: stop, diff it against the last known-good state, and don't build on top of it until the actual change is understood.

### 2026-09-10: 8 findings from a full project review, all fixed in the same PR

- **High**: digest email didn't respect `soldOut`/`lowTix` (now mirrors `index.html`'s `priceOrTicketsHTML` exactly).
- **High**: `index.html`'s `cardHTML()` had no fallback for an unrecognized venue code and would throw mid-`render()`, breaking the whole page — real risk given `shows.json` is scraped weekly by an automated agent. Now falls back to the raw code, matching a guard `worker.js` already had.
- **Medium**: `KV.list()` calls were unpaginated (silently truncates past 1000 keys) — added `listAllKeys()`.
- **Medium**: `myShows` had no length/count cap, reachable via the token-only `/api/star-show` endpoint — added `MAX_SHOW_ID_LENGTH`/`MAX_MY_SHOWS`.
- **Medium**: weak deploy gate — added the sanity-check CI step described above.
- **Low**: `/api/unsubscribe` had no confirm step (mail scanners auto-GET links) — added, matching `/api/star-show`'s existing pattern.
- **Low**: `/api/report-conflicts`'s `conflicts` array had no length cap, unlike every other caller-supplied input in the file.
- **Low**: the session token briefly sits in the URL after a magic-link redirect (`index.html` already strips it via `history.replaceState`, but a `Referer` leak to third-party loads — Google Fonts, Turnstile — was still possible in that window) — added `<meta name="referrer" content="same-origin">`.

### 2026-09-11: full-project audit, 6 findings, all fixed in PR #51

Ran on explicit request ("debug and clean up the entire project in all respects... check all security and stability aspects") via the `code-review` skill (whole-project pass, `max` effort) plus a manual security sweep on top. All 6 verified and fixed:

- **Correctness/data-race**: `index.html`'s `syncWithServer()` resolved `INITIALIZED` but not `STORAGE_READY` — a star-toggle's own `loadStorageOnce()` call, if it happened to be the first one after a fresh sign-in/sync, could re-read stale pre-sync `localStorage` and silently clobber the just-added star. Fixed by resolving `STORAGE_READY` too.
- **Stability/lockout**: `admin.html`'s `loadStats()` had no handling for a `401` (expired/invalid session) — the owner would get stuck on a raw error with no way back short of manually clearing site data. Now clears the stored session and re-renders the sign-in form.
- ~~**Dead code**: `cardHTML()`'s `isNew` parameter (drives the "New" badge) was hardcoded `false` at both call sites, so the badge could never render. Wired to a new shared `recentAddedCutoff()` helper.~~ **Reverted 2026-09-11**: the badge itself was never authorized product behavior — `isNew` is hardcoded `false` at both call sites again. The unrelated part of this finding stands: `recentAddedCutoff()` still exists and the "Added This Week" section still uses a 7-day cutoff (matching the digest email, which had drifted to 10 before this same PR).
- **Fragile config**: `admin.html`'s `WORKER_BASE_URL` still pointed at the raw `*.workers.dev` fallback URL. Switched to `api.chs.shownotice.com`, removing a silent dependency on `wrangler.toml`'s `workers_dev = true` staying enabled forever.
- **Performance**: `handleAdminStats` read KV in four sequential per-key loops (subscribers, unsubscribes, users, suggestions) — latency scaled linearly with total record count. Batched each loop via `Promise.all`.
- **Manual sweep, no fix needed**: re-confirmed Turnstile + 60s per-email rate limit + length caps + `<>` rejection are all intact on `handleSuggestVenue`, and spot-checked `escapeHtml()` usage on less-obvious `innerHTML` sites (venue/festival chips) — no further issues found.

### 2026-09-13: "Eric Lindell missing from Nov 5" — root cause was PR review latency, not a scraping gap (routine prompt updated, PR #73 merged)

User reported Eric Lindell (Charleston Pour House, Deck Stage, Nov 5) missing from the live site. Investigation, in order:

- **The show was already correctly found.** The same-day manually-scoped Pour House backfill run (fired earlier that session, see "Show data collection" below) had already fetched it directly from the venue's own page (`charlestonpourhouse.com/event/eric-lindell/`: doors 5:00 PM, show 6:00 PM, $35, Deck Stage) and included it in PR #73 — which was sitting **open and unmerged**, because the run touched 30 shows total, over the routine's own 15-shows-changed manual-review threshold. The live site (`main`) genuinely didn't have it yet, but not because it was missed — because nobody had reviewed and merged the PR in the ~2 hours since it opened.
- **A real, useful finding surfaced along the way regardless:** Charleston Pour House runs (at least) three stages — Main Stage, Deck Stage, and an outdoor Back Lot Stage — each independently bookable on the same date. A date with a marquee Main Stage act (Tommy Prine, same night) can have a completely separate, unrelated Deck Stage show, and Deck Stage shows are typically ticketed via `aftontickets.com` rather than the venue's usual flow. This wasn't actually the cause of the miss this time (the routine's own per-event-page crawl already handles it, and had already found Eric Lindell independently), but it wasn't previously written down anywhere, so it's now documented in the routine's own prompt (per-venue fetch-strategy row for Pour House, plus a new general "multi-stage venues" rule) so a future session or reviewer doesn't have to rediscover it.
- **Verified and merged PR #73** after independently re-running its stated validation (valid JSON, no duplicate `(venue, date, band)` triples, no unknown venue codes — all passed) and spot-checking its flagged judgment calls (the one price outlier, $225 for a fine-dining hybrid event, was accurately flagged as intentional). 30 shows landed on `main` in one squash-merge: 13 backfilled placeholder-data shows plus 17 newly discovered ones (Sep 19 – Dec 13), including the moe.phrey's naming question being resolved (confirmed as the venue's actual name for the collective, not a mistranscription of "Al Schnier").
- **Process fix actually applied:** the routine's own prompt (`trig_019xhSrKFCHTtKPoDoDsgogC`, updated via `update_trigger`) now says a large, expected diff from an explicitly-scoped catch-up run still isn't exempt from the 15-show review gate, but the PR description should say so clearly at the top — and calls out this exact incident as the reason: a correct answer sitting in an unmerged PR reads, from the outside, identically to a missed show. There's no automated nudge yet if a large routine PR goes unreviewed for a while; that's a real gap, not yet solved, worth a check-in mechanism if this recurs.
- **Process mis-step during this same investigation, corrected immediately:** an `update_trigger` call meant to carry the full amended prompt was accidentally sent with a placeholder string instead, briefly overwriting the routine's real prompt with garbage text. Caught immediately (the tool's own response echoes the prompt it just set) and fixed with a second, correct call before anything could fire against the broken version. Worth being deliberate/re-reading a large prompt string before submitting it via `update_trigger` — there's no diff/confirm step, it replaces the whole thing outright.

### 2026-09-13: Karina Rykman recorded on the wrong stage — a genuine data-entry error, not a website error

User reported Karina Rykman (Pour House, Oct 24) shown on the Deck Stage when she's actually on the Main Stage. Fetched her individual event page directly (`charlestonpourhouse.com/event/karina-rykman-3/`) — it unambiguously says Main Stage, and always has; this was a stage-attribution mistake from whichever earlier session first backfilled this show (likely the original "fixed directly as the flagged example" pass referenced in the now-resolved Pour House placeholder-data issue above), not a live scraper bug or a site-side change.

- **Fixed directly:** `s` corrected to `"Main Stage"`; also backfilled `dop: 30` (the venue's page lists $25 advance / $30 day-of, and only the advance price had been stored) while already on the page, per the standing advance/day-of rule.
- **Spot-checked the rest of the dataset for the same error class:** cross-referenced every other Pour House show independently confirmed as Main Stage (via an earlier direct fetch of the venue's own combined listing, in the Nov 5 investigation above) against its `s` field in `shows.json` — all 16 others matched correctly. This looks like an isolated one-off, not a systemic pattern, though only that one batch was checked; a wrong `s` value elsewhere in the data can't be fully ruled out without checking every show's own event page individually.
- **Process fix applied:** added a rule to the routine's own prompt: the `s` (stage) field must be confirmed against a show's own individual event page, never inferred from its position on the combined multi-stage `/shows/` listing — that combined page interleaves cards from all three stages, and skimming it top-to-bottom is exactly the kind of mistake that produces this class of error. Cites this incident by name so it isn't rediscovered from scratch.

### 2026-09-17 → 2026-09-24: routine auto-merge policy — tried, caused a real bug to ship, reverted

Full story consolidated here; see the policy-change note under "Show data collection" above for the standing-rule summary.

- **2026-09-17:** a separate session applied a user directive to remove the routine's entire manual-review gate (see "Karina Rykman"/"Eric Lindell" incidents above for the gate this session originally built), so every run merges automatically once basic validation (valid JSON, no duplicate triples, no unknown venue codes) passes — no matter the diff size, date-shift magnitude, price outlier, or source reliability. Flagged to the user proactively the same day rather than treated as silently settled, given it reversed deliberate safety work; the user said to keep it as drafted but wanted to revisit later, so a self-reminder was scheduled for one week out.
- **2026-09-24, reminder fired:** reviewed every data-refresh PR merged since the change (#91, #92, #93). Found a real, concrete cost: PR #91 backfilled Refinery show times from `charlestonlivemusic.com`, which mislabels genuinely-local Eastern times with a `+00:00` (UTC) suffix — applying the (normally correct) UTC→Eastern conversion shifted several real shows' times 4 hours early. This merged with zero review and stayed wrong on the live site until **the user** noticed and reported it (fixed same-day in PR #92, which also found the root cause: the source site also emits `1970-01-01T00:00:00+00:00` placeholder timestamps for events with no real date entered, which an earlier pass had wrongly treated as real data). Also found, same batch: 2 Gaillard Center shows in PR #93 sourced from web search alone (no actual fetch — GC's own site had gone behind a SiteGround CAPTCHA) merged with just a flag, and an unresolved same-date/time/stage double-booking at Slightly North of Broad likewise merged unreviewed.
- **User's decision, once shown this:** keep the review gate reverted (see below) and set up a standing way to review PRs without needing to know GitHub — "I need a simple way of reviewing. i don't know where to do this."
- **Fixed 2026-09-24:** restored the routine's "Validation before committing" / "When NOT to auto-merge" sections — which, worth noting, had gone missing from the stored prompt entirely on 2026-09-17, not just been logically disabled by the policy note; step 6 referenced a "Validation before committing" section that no longer existed anywhere in the document. Rebuilt with the original checks plus two new ones aimed directly at what slipped through: shows sourced from web search alone, and unresolved conflicts between two shows. Also fixed a dangling unclosed markdown code fence (the BeatGig GraphQL query block) that predated this session's edit but would have swallowed every section after it once appended to.
- **New standing mechanism:** a second Routine, `Review Show Notice routine PRs` (`trig_0169Yf4SsUr8cSdW2Qy4qeG3`, Thursdays 9am ET, fires into this same interactive session), checks for any PR the gate leaves open and summarizes it in plain language right in chat, asking for an explicit yes/no per PR before merging — so "reviewing" never requires opening GitHub. This is the direct answer to the user's "I don't know where to do this."
- **Takeaway:** an autonomous data pipeline's safety gate is not a one-time design decision — it can be changed by any session with `update_trigger` access, and the interactive session watching over it needs its own mechanism (here, a scheduled self-reminder) to notice and revisit that rather than assuming a prior decision holds indefinitely.

## Security posture (audited and fixed)

- **All rendered data is escaped.** `index.html` and `admin.html` both have an `escapeHtml()` helper applied to every interpolated value — this matters more than it used to, since `shows.json` is now written by an automated agent scraping venue websites, making a malformed venue listing a plausible injection path, not a hypothetical one.
- **URLs are validated before reaching an `href`.** `safeUrl()` in `index.html` (and an equivalent inline check in `worker.js`'s digest email builder) rejects anything that isn't `http://` or `https://`, blocking `javascript:`/`data:` URLs.
- **Input validation is applied on both sides.** Favorite artist names, venue suggestions, and (added 2026-09-12) Contact Us messages reject angle brackets and enforce length limits in both the browser and the Worker. Server-side is the real gate; client-side just prevents the value from rendering locally before any round-trip.
- **Contact Us is the one fully public, unauthenticated write endpoint added since the initial audits** (`/api/contact` — no session required, unlike venue suggestions) — gated the same way sign-in requests are: Turnstile + a per-IP rate limit (see KV table above), plus the same length/angle-bracket validation as everything else. No email address is exposed anywhere in the page for a bot to scrape in the first place, which was the actual motivation for adding this form.
- **Admin authorization is server-enforced** (`handleAdminStats` checks the session email against `OWNER_EMAIL`), not merely hidden in the UI — correct and should stay that way, since that endpoint exposes other users' email addresses.
- The per-IP sign-in rate limit's non-atomic KV race (see KV table above) is a known, accepted limitation, not an oversight.

## Domain migration (2026-09-11): gigalertchs.com → CHS.ShowNotice.com

The project rebranded from "GigAlert CHS" to **Show Notice** and moved off `gigalertchs.com` onto `chs.shownotice.com`, following a pre-written migration plan. Summary of what changed:

- **GitHub Pages custom domain**: `CNAME` updated from `gigalertchs.com` to `chs.shownotice.com` (PR #27).
- **Site/Worker config**: `SITE_URL`, `WORKER_BASE_URL`, and every hardcoded reference in `index.html`/`admin.html` repointed at the new domain (PR #28).
- **Worker custom domain route**: `api.chs.shownotice.com` wired via `[[routes]]` in `wrangler.toml` (`custom_domain = true`, `zone_name = "shownotice.com"`) so the API isn't dependent on the `*.workers.dev` fallback.
- **Old domain kept alive as a redirect, not decommissioned**: `gigalertchs.com` now 301-redirects to the new domain via a Cloudflare Redirect Rule — confirmed working via a direct `curl -I` test (301 status, correct `Location` header) and confirmed by the user on both desktop and mobile browsers, so existing bookmarks/links/search results keep working.
- **Email sender identity**: `RESEND_FROM_ADDRESS` updated to `Lowcountry Show Notice <shows@shownotice.com>` (PR #31); owner contact address is `admin@ShowNotice.com`.
- **Stale infrastructure removed**: the old `show-tracker-api.gigalertchs.workers.dev` Worker route/domain entry was confirmed no longer needed (everything now goes through `api.chs.shownotice.com`) and removed from the Worker's Domains panel in the Cloudflare dashboard — a dashboard-side cleanup step, not a `wrangler.toml` change.
- **Cloudflare Web Analytics and Zone-level features** (bot detection, WAF, etc.) enabled on the new `shownotice.com` zone as part of the same session — see "Admin analytics panel & Cloudflare integration" below for what actually got wired into the app itself.

If a future session finds any remaining `gigalertchs` references (in code comments, email templates, or elsewhere), that's leftover from the old identity and should be updated to match — the redirect exists so *external* links keep working, not so the codebase can keep referencing the old name internally.

## Admin analytics panel & Cloudflare integration (2026-09-11)

`admin.html` was substantially built out from a bare stats page into a full analytics dashboard, driven by the user's "what else of use can we track without getting too esoteric or nosy" → "add them all" → "add week/month/year/all columns" progression. Everything below is server-computed in `handleAdminStats` (`worker.js`) and rendered in `admin.html`; **no client-side tracking/cookies were added** — all app-level numbers are derived from existing KV records (subscribers, users, suggestions) plus the timestamp-backfill fields described in the KV table above, and the Cloudflare numbers come from Cloudflare's own edge/zone data, not client-side beacons.

**App-level stats, each broken out by rolling week/month/year/all-time windows** (a `windowCounts`/`windowGroupedCounts` helper pattern — last 7/30/365 days from *now*, not calendar periods):
- Subscribers (signups, unsubscribes, net).
- Accounts created, and per-account My Shows/Favorite Artists counts.
- Digest email engagement (opens/clicks), sourced from Resend's webhook via `RESEND_WEBHOOK_SECRET` (see Secrets section) — this is why that secret and its Svix-signature verification exist.
- Top favorited artists and venues.
- Top starred shows.
- Venue suggestions submitted.

**Cloudflare zone data** (via `fetchCloudflareZoneAnalytics()`, `worker.js`, using `CLOUDFLARE_ANALYTICS_TOKEN` and `CLOUDFLARE_ZONE_ID` against Cloudflare's GraphQL Analytics API, `httpRequests1dGroups`):
- Requests / page views.
- Threats blocked.
- Cache hit ratio and error rate.
- Top countries by traffic — rendered with full country names, not ISO codes.
- The query window is deliberately capped at 358 days, not the full year, to stay under Cloudflare's 52-week-1-day-1-hour query-span limit — querying a full calendar year would occasionally exceed it depending on leap-year/DST alignment.

**Layout**: reworked from a flat stat dump into grouped sections with wider numeric columns (so larger numbers don't get clipped as the dataset grows) and click-to-sort tables.

**What this doesn't cover** (deliberately, per the "not too esoteric or nosy" framing that shaped this feature): no per-user browsing/click tracking, no IP-level user profiling, no third-party analytics/ad trackers. Everything shown is either an app-necessary record (who's subscribed, what they starred) or Cloudflare's own aggregate edge telemetry.

## Digest email

Sent weekly via Cron Trigger (`sendDigestToAllSubscribers`, called from the Worker's `scheduled()` handler — cron schedule lives in `wrangler.toml`, currently `0 17 * * thu` = 17:00 UTC Thursdays (1pm ET), changed 2026-09-12 from the earlier `0 19 * * wed` = 19:00 UTC Wednesdays, which was itself a fix for an even earlier `0 18 * * 3` that would have silently shifted the send time by an hour on deploy). Changed together with the separate weekly research routine's own schedule (not part of this repo — see "Show data collection" below), which now runs 3am ET Thursdays, a few hours ahead of this send, so a fresh data pull is in `shows.json` before that week's digest goes out. Personalized per subscriber: pulls their `myShows` from KV, renders a dedicated "My Shows" section above "Added This Week" and "Everything Else Coming Up," with a one-click star/unstar link on every card (`/api/star-show`, reusing their unsubscribe token as auth).

Header uses a double amber-border treatment matching the site's `.b2-frame` CSS, reproduced with nested `<table>`s since HTML email doesn't support `clip-path` (site's cut corners → square) or reliably render `rgba()` in old Outlook (site's translucent inner border → solid `--amber-dim` `#8a672a`). Font is Georgia (web-safe serif), deliberately not the site's Google Font. (Note: an earlier *draft mockup* of the main site's time display had also used Georgia and caused AM/PM to wrap — that was a bug and was fixed in `index.html`'s own card display, which now uses a condensed font for the time number with AM/PM as a smaller inline suffix on the same line. The digest email's use of Georgia for its header is a separate, deliberate, and still-current choice — don't conflate the two.)

`/api/digest-preview` (session-gated, your own digest) and `/api/test-send-digest-now` (session-gated, owner-only, fires a real send to every subscriber) exist for manual testing without waiting on the Cron Trigger.

## Account model: sign-in, subscribe/unsubscribe & Contact Us (2026-09-12, PRs #66–#68)

Driven by the user's explicit request to stop putting a plain email address on the page ("I don't want that much spam") and to make subscribe/unsubscribe state track the sign-in checkbox exactly, rather than an earlier implicit-subscribe behavior.

- **Combined "About & Contact" footer panel.** The old separate "Show details" disclaimer note, the privacy-details toggle, and (new) a Contact Us form are now one panel behind a single `#privacy-toggle-btn` labeled "About & Contact." No visible admin/owner email address anywhere on the page — Contact Us is now the only way a visitor reaches the owner from the site itself.
- **`POST /api/contact` (new, `handleContactUs` in `worker.js`)** — public, unauthenticated, Turnstile-gated, per-IP rate-limited (`contact-ratelimit:<ip>`, same pattern as the other rate limits in the KV table above). Body: `{ email?, message, turnstileToken }` — `message` required, ≤2000 chars, rejects `<`/`>` (same input-validation convention as favorite names/venue suggestions); `email` optional (a visitor can send a note without giving one — stored as `(no email provided)` where displayed). Stores `contact:<uuid>` → `{ email, message, submittedAt }` in KV and emails the owner (`buildContactEmailHTML`/`sendContactEmail`, subject "New Contact Us message").
- **Admin panel mirrors this** — `handleAdminStats` now also returns `contactMessages: { count, list, window }` (parallel to the existing `suggestions` block), and `admin.html` renders a "Contact Us messages" block in the Community section right alongside "Venue suggestions."
- **Explicit subscribe/unsubscribe semantics, replacing an implicit `ensureSubscribed()`.** The sign-in form has a `#signin-subscribe-check` checkbox ("Send me a once-a-week email of new shows"), checked by default. `subscribe: boolean` now flows end to end: `POST /api/auth/request-link` accepts it and stores it on the token (`JSON.stringify({ email, ...(returnTo?{returnTo}:{}), subscribe })` — an `undefined` value here is simply dropped by `JSON.stringify`, so an older/no-checkbox client degrades safely to "leave subscribe state untouched"); `GET /api/auth/verify` reads it back off the token and calls one of two new helpers:
  - **`subscribeEmail(email, env)`** — writes `subscriber:<email>`, always clears any `unsubscribed:<email>` marker (an explicit opt-in overrides a past unsubscribe, same as clicking "Subscribe to email updates" directly).
  - **`markUnsubscribed(email, env)`** — looks up `subscriber:<email>` first so it can delete that record's own `unsubtoken:<uuid>` too (not just the `subscriber:` key itself), then sets the permanent `unsubscribed:<email>` marker. Shared by three call sites now: the sign-in checkbox (when unchecked), `/api/unsubscribe`'s confirmed click, and (indirectly) anywhere else that used to call the old `ensureSubscribed()` — which no longer exists.
  - `handleSubscribe`/`handleUnsubscribe` (the standalone `/api/subscribe` POST and `/api/unsubscribe` GET-with-confirm routes) were refactored to call these same two helpers rather than duplicating the KV writes.
  - **The visible "Unsubscribe" note/link in the sign-in UI was removed** — unsubscribing is now just "sign in and uncheck the box," not a separate control. The digest email's own footer "Unsubscribe" link (`buildDigestEmailHTML`, confirmed already present at the time of this change, not newly added) remains the mechanism for someone who wants to unsubscribe without ever signing in again.
  - **"No spam, ever." note** sits next to the sign-in control (`#no-spam-note`, toggled by `renderAccountUI()` — hidden once actually signed in, since at that point it's not a standing claim about the account, just reassurance about the act of giving out an email address in the first place).
- **Past-show cutoff fixed to drop same-day, not with a 1-day grace period.** `pastCutoff`/`upcomingShows()` in both `index.html` and `worker.js` now use `new Date().toISOString().slice(0,10)` directly as the inclusive-today boundary (was previously `Date.now() - 1*24*60*60*1000`-style logic that kept yesterday's shows visible for one extra day) — a real off-by-one, duplicated identically in both files per the existing "these must match" convention (see `showId()` note in Data model above), fixed in both at once.
- **Magic-link email copy** (found undocumented while writing this section, folded in here since it's the same account-flow surface): subject is "Your Show Notice sign-in link," body says "sign in to your Show Notice account," and the link itself is anchored as "Sign In Here" rather than displaying the raw URL — matches the "Website" link-text convention adopted for venue pages in the nav redesign below (display a label, not a bare URL, wherever a link's destination is already obvious from context).

## Filter bar (browse view) — superseded, see "Nav/layout redesign" below

**This section describes filter mechanics that no longer exist in the code as of the PR #19 header/nav redesign** (confirmed 2026-09-12 by searching `index.html` for its date-range/toggle markup — none found) but were never corrected here at the time. Left below, struck through in spirit, for history; do not treat it as current behavior.

~~Controls: date range (This week / This month / All), a single-venue selector (no "exclude this venue" mode — tried and explicitly removed as unnecessary), and a "Favorite artists only" toggle. Default on load: all dates, all venues, favorites off. Applies only to the browse views, never My Shows/Favorite Artists.~~

**Current reality (2026-09-12, PR #70):** there is no date-range or favorites-only filter. The only calendar-narrowing control is the **All Shows venue filter** — see "Nav/layout redesign" below — a multi-select venue/festival picker scoped to the All Shows section only, same as the old bar's stated scope. A show that's in My Shows still appears normally in All Shows too — there is still no exclusion logic anywhere that removes a show from the full list just because it's also in My Shows.

## UI dialogs (custom modals)

As of PR #17, `index.html` has **zero native `alert()`/`confirm()` calls** — every one was replaced with a small custom modal system so buttons can say "Yes"/"No"/"OK" (a browser's native `confirm()` is always "OK"/"Cancel" and can't be relabeled) and everything stays visually consistent with the site's own styling (same panel look as the filter sheet, same `filter-btn-clear`/`filter-btn-apply` button classes).

Three reusable, Promise-based helpers (defined near `attachWatchHandlers()`):
- **`showAlert(message)`** — single "OK" button. Used for: the blocked-localStorage warning, favorite-name validation, every sign-in-flow message (empty email, request errors, test-mode magic link, "Check your email," invalid/expired link), the subscribe flow, and the venue-suggestion flow.
- **`confirmYesNo(message)`** — "Yes"/"No" buttons, resolves `true`/`false`. Dismissing via the backdrop counts as No. Used for the single-artist star-to-favorite prompt (see below).
- **`pickArtistsToFavorite(candidates)`** — a checklist of artist names (all pre-checked), "Add Selected"/"Skip" buttons, resolves the array of names left checked (`[]` on Skip or backdrop-dismiss). Used for the multi-artist star-to-favorite prompt (see below).

Each has its own `#<x>-overlay` div in the HTML (`confirm-overlay`, `alert-overlay`, `artist-picker-overlay`), sharing `.modal-overlay`/`.modal-box`/`.modal-message` CSS classes. If a new confirmation/input dialog is ever needed, extend this pattern rather than reaching for a native `alert()`/`confirm()`/`prompt()`.

## Header layout & mobile responsiveness (PRs #12–#14, #16–#17, #19, #21–#24, #69–#70)

A same-day 2026-09-10 session made several changes to the page header and utility links, mostly driven by live mobile screenshots — culminating in a full redesign (#19) that replaced most of the earlier iterations outright. History, oldest first:

- **Riverfront Park venue-page mobile overflow (PR #12).** Riverfront Park's venue-site URL (`northcharleston.org/visitors/attractions_points_of_interest/riverfront_park/index.php`, ~97 chars) is far longer than any other venue's (next longest ~64 chars) and had no `overflow-wrap` rule — the unbroken `<a>` text forced the whole page wider than a mobile viewport instead of wrapping, squishing the visible layout to the left. Fixed with `overflow-wrap:anywhere` on both `.venue-site-link` and `.venue-address`. Still current.
- **Utility-row reorg (PR #13), sizing (PR #14), alignment fix (PR #16), and mobile-columns fix (PR #17) — all superseded by #19, see below.** These introduced and then repaired a two-column `.utility-cols`/`.utility-col` jump-link layout (My Shows/Full List left, Manage favorite artists/Suggest a venue right) plus a `.utility-row` wrapper for sign-in/Last-Updated. None of those classes exist anymore as of #19 — kept here only as history in case an old branch/PR references them.

### Full redesign (PR #19): sticky banner, sticky section headers, nav pills

- **Sticky title banner.** `.b2-frame`/`.tagline` live in a new `.banner-sticky` div, now a **direct child of `.wrap`**, positioned *before* `<header class="top">` rather than inside it — a `position:sticky` element can only stay pinned as long as it hasn't scrolled past the bottom of its own containing block, and `header.top` is short, so nesting it there caused it to un-stick after a short scroll. `.wrap` spans the whole page, so that's its containing block now. A `--banner-h` CSS custom property, kept in sync by `updateBannerHeight()` (re-run on load, `window resize`, and `document.fonts.ready` — Bebas Neue loads async and can shift the banner's height slightly after first paint), tells every section header exactly how far down to pin itself.
- **Sticky section headers.** `.sticky-head` (`top:var(--banner-h)`) wraps the title + links row of five sections — Added This Week, My Shows, All Shows, Favorite Artists, Suggest a Venue — so each section's own heading stays visible while its content scrolls underneath, directly below the still-pinned title banner.
- **"Full List" renamed to "All Shows," now a real section.** `#main-list` (previously a bare `<div>` with no heading at all) is now wrapped in `<section id="all-shows-section">` with its own `.section-title`. The header nav pill (see below) and jump-to-all-shows logic were renamed to match.
- **Chained "Back to top" + "next section" links.** Every one of the five sections above gets a `.section-title-links` block: a `.back-to-top-link` (existing generic handler) plus — except on the last one, Suggest a Venue — a `.jump-next-link` with `data-jump-next="<next-section-id>"`, chained Added This Week → My Shows → All Shows → Favorite Artists → Suggest a Venue. One generic `[data-jump-next]` click handler drives all of them.
- **Header nav pills.** The old two-column jump-link layout is gone. In its place: a single horizontally-scrollable row (`.nav-chip-row`) of 4 pills (`.nav-chip`, styled like `.venue-chip` but with a fixed teal border/text instead of per-venue color) — **My Shows**, **All Shows**, **Favorite Artists**, **Suggest a Venue** (note the renamed/recapitalized labels — no longer "Manage favorite artists" or "Suggest a venue"). On mobile the row is `justify-content:flex-start` (not centered) specifically so nothing before the visible viewport becomes unreachable — you can't scroll left past position 0, so centering a row wider than the screen would strand its start.
- **Status row.** Sign-in/out and Last Updated moved into their own `.status-row` below the nav pills, with its own hairline top border and `justify-content:space-between` — this is also what let the `align-self:flex-start` overrides from PR #16 finally get removed entirely; the flex layout doesn't need them.
- **Last Updated itself is unchanged** — still reads `data.dataUpdatedAt` from `shows.json` as-is (the date the file was last edited, whatever the reason); no separate "last full redesign/update" field was added.

### Rounding out the redesign (PRs #21–#24)

A second same-day 2026-09-10 pass, driven by more live mobile screenshots and follow-up requests against the #19 redesign:

- **Divider + sticky venue name (PR #21).** `.nav-chip-row` got the same `border-top`/`padding-top` divider convention the old `.utility-row` used, separating the venue/festival pills above it from the 4 nav pills below. Separately, a venue's own show page (`showVenueView`) wraps its `<h2>` venue name in `.sticky-head` (same class every section header uses), so it pins below the title banner while that venue's shows scroll underneath.
- **Mobile nav pill sizing (PR #22).** `.nav-chip`/`.nav-chip-row` sized down on mobile only (`font-size` 12px→11px, `padding` 6px 14px→5px 11px, `gap` 8px→6px) so the first 3 pills (My Shows, All Shows, Favorite Artists) fit on a 375px-wide screen with the 4th (Suggest a Venue) left off-screen but reachable via the row's existing horizontal scroll. Desktop untouched — it already fit all 4.
- **Sticky date headers (PR #23, extended to every list in PR #24).** `.day-head` (the "Saturday, September 12"-style date banner above a group of same-day shows) is now `position:sticky` everywhere shows are grouped by date, so the date stays visible while that day's shows scroll by, handing off to the next date at the right moment. This now covers **all four** show lists:
  - All Shows and a venue's own page (#23)
  - My Shows and Added This Week (#24) — both were flat lists (an inline date per card, via a `flatDate` mode on `cardHTML` that's now been **removed entirely**, along with its `.flat-date` CSS, since nothing calls it anymore) rather than day-grouped; switched to the same day-grouped rendering as the other two, via a new shared `groupByDayHTML()` helper (the grouping/sorting logic used to live only in the All Shows render step, now used by all four lists instead of being copy-pasted).

  Each context's `.day-head` pins at `calc(var(--banner-h) + <that context's own .sticky-head height>)` so it stacks directly below the still-pinned banner and section/venue header rather than overlapping it. Four parallel CSS vars track those heights, same pattern as `--banner-h` itself: `--added-this-week-head-h`, `--my-shows-head-h`, `--all-shows-head-h`, `--venue-head-h`. `.day-head` also switched from a `background` shorthand (whose gradient fades to fully transparent on the right) to a solid `background-color` with the gradient layered on top as `background-image` — sticky content needs an opaque backdrop or cards scrolling underneath show through the transparent tail.

  **A real bug worth remembering:** `showVenueView()` originally measured `--venue-head-h` *before* `viewEl.style.display` was set to `'block'`, so `offsetHeight` read `0` — anything inside a `display:none` ancestor measures zero regardless of its own size. Same class of bug nearly recurred for `--added-this-week-head-h`, since that section also starts `display:none` and only switches on inside `render()` when there's something recent to show — its height is measured from inside `render()` itself, right after the display flip, rather than from the page-load/resize/`fonts.ready` handlers the other three heights use (which run before that section is necessarily visible). **Any future sticky-height measurement on an element that can start or become `display:none` needs to happen after it's confirmed visible, not just after its markup exists.**
- **Sticky "← Back" link on venue pages (PR #24).** Used to sit above `.sticky-head` as a separate block, so scrolling into a venue's show list scrolled it out of reach. Moved inside `.sticky-head` alongside the venue name (same `.section-title-row` layout the rest of the redesign uses for title+links rows), label shortened to "← Back" to fit compactly.

**Takeaway for future layout work:** screenshot-test at actual mobile widths (375px/390px/430px covers the common range) before considering a responsive layout change done — several items in this section's history (#12, #17, #22) were regressions/gaps that only surfaced in a real mobile screenshot, not in desktop-width review. For anything involving `position:sticky`, verify the containing block is tall enough to hold the pin for the whole scroll range you expect (the #19 banner fix), measure heights only after the element is confirmed visible — not just present in the DOM (the `display:none` bug above, twice), and test at real scroll depth, not just a quick scroll-and-glance — a scripted test that fires `window.scrollTo` and screenshots too soon can catch a smooth-scroll animation mid-flight and look like a bug that isn't one (use `behavior:'instant'` or wait long enough, and cross-check the element's actual `getBoundingClientRect()` before trusting a screenshot alone).

### Nav/layout redesign, 2026-09-12 (PRs #69–#70)

Two rounds against the same user request, run back to back the same session: PR #69 did the structural reshuffle (moved sections/controls around, first cut of a venue filter as a native `<select>`); PR #70 was pure follow-up polish once the user reviewed #69 live (underline the sign-in link, relocate Last Updated, restyle the filter as a pill matching the other jump-links, make it multi-select, re-center the top pills). Both merged; treat this as the current, final state — don't reconstruct #69's single-select `<select>` version, it no longer exists.

- **Top nav pills reordered and reduced to three:** All Shows, My Shows, Favorite Artists (previously My Shows, All Shows, Favorite Artists, Suggest a Venue). **Suggest a Venue pill removed from the top nav** — the section itself (`#suggest-venue-section`) still exists and still works (Contact Us, added the same day per the account-model section above, is a related but separate feature — Suggest a Venue is specifically for requesting a venue be added to the tracker, still session-gated), it's just no longer pinned to the top nav or cross-linked from Favorite Artists' section header; reachable only by scrolling to it. Centered via `justify-content:center` in both the desktop rule and the (previously `flex-start`, now also `center`) mobile media-query override — the old `flex-start` was deliberate back when a 4th pill could overflow off-screen with no way to scroll left to it (see PR #22 above); with only 3 pills that constraint no longer applies.
- **Venue/festival pill directory (`#venue-links`/`#festival-links`) moved from the page header down to a new `#venue-directory-section` at the very bottom of the page**, just above the footer — user's own framing: "the venue pill list is getting large... might remove that... we'll see," so this is treated as a temporary/reversible relocation, not a permanent design decision. No title/heading was added to this section — kept exactly as minimal as it was in the header, just relocated.
- **Sign-in relocated into My Shows.** The `#account-btn` (sign-in/sign-out button), the `#no-spam-note` reassurance text next to it, and the entire sign-in form (`#sign-in-section` — email input, subscribe checkbox, Turnstile, submit button) all moved out of the page header and into the top of the My Shows section, above `#watch-list`. `renderAccountUI()` needed no changes — it still targets these elements purely by ID, which didn't change, only their position in the DOM. `#account-btn` also picked up `text-decoration:underline` (PR #70) so it reads as a link/control rather than just colored text, in both its signed-out and signed-in ("Signed in as X · Sign out") states.
- **"Last updated" moved out of the header entirely, down into the footer** (PR #70) — now the very last thing on the page before the footer's own buttons, rather than sharing a header status row with the (now-relocated) sign-in line.
- **My Shows' own section-header pills changed to "All Shows ↓" and "Favorite Artists ↓"** (jump-links, `data-jump-next`), replacing what used to be "Back to top ↑" + "All Shows ↓" — since the top nav (now 3 pills, always visible) already covers "back to top," a dedicated pill for it here was redundant.
- **All Shows' section-header pills reduced to just "Back to top ↑"** — its old "Favorite Artists ↓" jump-link was dropped since Favorite Artists is now reachable both from My Shows (above) and the top nav.
- **Favorite Artists' section-header pill reduced to just "Back to top ↑"** — its old "Suggest a Venue ↓" jump-link was dropped along with Suggest a Venue's top-nav prominence (see above); nothing else links to Suggest a Venue anymore, it's scroll-reachable only.
- **All Shows venue filter — a multi-select checkbox panel, not a native `<select>`.** Sits between the "All Shows" title and the "Back to top" pill in that section's `.section-title-row` (a `.venue-filter-dd` wrapper, `position:relative`). Collapsed state is a pill (`.venue-filter-toggle`) styled identically to `.back-to-top-link`/`.jump-next-link` (same shared CSS selector, extended to include it) — label shows "Show all venues ▾" when nothing's selected, the single venue's name when exactly one is, or "`N` venues selected ▾" for more than one. Clicking it toggles a `.venue-filter-panel` (`position:absolute`, `z-index:20`, opaque background) open — **it deliberately overlaps and visually covers the Back to top pill next to it rather than pushing the row's layout around**, per the user's own spec ("it can widen out to cover up the Back to Top pill until collapsed"); collapses again on re-clicking the toggle or on any click outside `.venue-filter-dd` (a document-level click listener checks `dd.contains(e.target)`). Selection state lives in a module-level `VENUE_FILTER` (a `Set` of venue/festival codes, empty = show all) plus `VENUE_FILTER_OPEN` (open/closed, tracked separately from the Set so an unrelated `render()` call — e.g. from a star toggle elsewhere on the page — doesn't slam the panel shut mid-selection). Filtering itself is a plain `VENUE_FILTER.size ? sorted.filter(s => VENUE_FILTER.has(s.v)) : sorted` inside `render()`, applied only to `#main-list` (All Shows), same scope the old dead filter-bar docs above claimed. **Gotcha for anyone testing this with Playwright/automation:** each checkbox's own `change` handler calls `render()`, which rebuilds the panel's entire `innerHTML` — a stale element handle to a checkbox (e.g. from an earlier `$$()` call) will be detached by the time a second interaction runs against it, and Playwright's `.check()` specifically fails on this (it re-verifies checked state against the same handle after the click); `.click()` doesn't have that extra verification step and works fine. Re-query fresh handles between interactions rather than reusing one across multiple state changes.
- **Venue pages: site link text changed from the raw URL to "Website."** `showVenueView()`'s `siteLine` now renders `<a href="${venueSite}">Website ↗</a>` instead of printing the URL itself as the link text — same href, just a label instead of exposing the raw address inline (mirrors the magic-link email's "Sign In Here" convention noted in the account-model section above).
- **Venue pages now pick up that venue's own accent color** (the same `color` field already driving that venue's pill border/text in the directory) for its `<h2>` name, the "← Back" link, the site-link text, the address-link hover state, and a 3px top border strip on the whole `.venue-view` block — replacing what was previously a uniform amber treatment on every venue page regardless of which venue it was (raised by the user as "the colors at the top seem too uniform"). Implemented via a `--vc` CSS custom property set with `viewEl.style.setProperty('--vc', venue.color)` inside `showVenueView()`, referenced throughout `.venue-view` CSS as `var(--vc, var(--amber))` so a venue with no `color` set still falls back to the original amber look. **Gotcha:** when a venue has no color, the code calls `viewEl.style.removeProperty('--vc')` rather than setting it to `''` — a CSS custom property explicitly set to an empty string still counts as "set" for `var()`'s fallback logic (the fallback only kicks in when the property is genuinely absent), so setting it to `''` would have made the color rule resolve to nothing/inherit instead of falling back to amber as intended.
- **One small sitewide polish item, deliberately minimal:** a slightly richer glow on the title banner text (`text-shadow`, two layers instead of one) — added in response to a mid-message aside ("need to work on presentation for the entire site... need just a little more energy. Not too much tho") that was otherwise interpreted narrowly (as being about the venue-page color fix above) since the rest of that sentence was never completed. Explicitly flagged to the user as an inference, not a confirmed instruction — if a broader sitewide visual pass is actually wanted, get specifics rather than assuming this covers it.

## My Shows / Favorite Artists model

- Two independent lists, deliberately decoupled. Favoriting an artist (Favorite Artists) always surfaces their upcoming shows in My Shows automatically. Favoriting a single show does not add the artist to Favorite Artists, and vice versa (this reversed an earlier, coupled behavior that used to exist in production).
- Un-favoriting an artist does not retroactively remove shows already auto-added to My Shows because of that artist — those stay until manually removed. Deliberate, not an oversight.
- Storage keys were renamed from `watchlist` to `my-shows` (client-side key, and the wire/KV field sent to the Worker, now `myShows`) — done deliberately while there were few enough users that no data migration was needed. Any future schema rename needs to touch `index.html`, `worker.js`, AND `admin.html` together (`admin.html` reads a `myShowsCount` field derived from this).
- **Favorite matching checks openers, not just the headliner (PR #10).** `matchesFavorite(bandName)` does substring-containment matching (both sides normalized: lowercased, non-alphanumeric collapsed to spaces) — a favorite matches if it's a substring of the show's artist name, e.g. favoriting "Trombone Shorty" matches "Trombone Shorty & Orleans Avenue." This originally only ever checked a show's `b` field, which meant a favorited artist buried in a multi-artist show's `o` field (festivals put the event name in `b` and the real lineup in `o`) never matched anything — favoriting "Darius Rucker" didn't pick up Riverfront Revival. Fixed via `showMatchesFavorite(s)`, which checks `b` first, then splits `o` the same way `cardHTML()` does for rendering and checks each name individually. Both the "Favorite artists only" filter and the auto-add-to-My-Shows logic in `render()` use `showMatchesFavorite`, not a raw `b`-only check, going forward.
- **Storage-reliability fixes (PR #11).** Two gaps in the signed-out storage path (localStorage is the *only* persistence layer when not signed in — no server round-trip):
  1. `saveMap()` used to swallow `localStorage.setItem` failures silently (`catch(e){}`). Some browser contexts — Safari Private Browsing chief among them — block all localStorage writes. The UI still showed the change as saved regardless (it renders off the in-memory array before the write even runs), with zero indication it hadn't actually persisted. Now warns once per page load via `showAlert()` and points at signing in as a fallback (a signed-in user's data also round-trips to the server, so it isn't dependent on localStorage working).
  2. The initial load of `MY_SHOWS`/`FAVORITES` from storage only ever ran inside `render()`'s first call, behind the `shows.json` fetch — but `#fav-input`/`#fav-add-btn` are wired up immediately at parse time, so a click on a slow connection could theoretically race the load. Consolidated into a single memoized `loadStorageOnce()` that `render()`, `addFavorite()`, and the star-toggle handler all await, so there's one source of truth instead of independent loads that could clobber each other.
- **Star-to-favorite-artist prompt (PRs #15, #17).** Starring a show (adding it to My Shows) now offers a one-tap way to also add the artist to Favorite Artists — opt-in, never asked when un-starring, and skipped entirely for an artist that already matches an existing favorite (via `matchesFavorite`, so no repeat prompts). Wired through two new attributes on the star button in `cardHTML()`: `data-band` (`extractArtistName(s.b)`) and `data-lineup` (the raw `s.o` field). Two paths:
  - **Single-artist shows:** `confirmYesNo()` — `Also add "<artist>" to your Favorite Artists?`
  - **Multi-artist/festival shows** (same "is this actually a list" check `cardHTML()` already uses to decide between opener chips and inline "w/ opener" — `o` splits into more than one comma-separated part, each ≤60 chars): `pickArtistsToFavorite()` shows a checklist of the event name (`b`) plus every individual lineup artist parsed from `o`, all pre-checked, with any already-favorited names excluded from the list entirely. Selecting a subset and confirming adds only those. This is what lets a show like Riverfront Revival (`b`: "Riverfront Revival", `o`: 17 comma-separated artist names) offer each artist individually, rather than only the event name itself.

## Show data collection (the weekly research routine)

A scheduled Claude Code Routine ("Show Tracker Weekly Update") fetches each tracked venue's own site, diffs against `shows.json`, and opens a PR with the changes, pushed to a `claude/`-prefixed branch. This repo doesn't contain the routine's prompt/config — the sections below summarize it for context when reviewing its PRs. The routine's own instructions explicitly scope it to `shows.json` only ("Don't touch index.html, worker.js, or admin.html — this task is data-only") — confirmed as of 2026-09-10 that the frontend UX session (PRs #10–#17, all `index.html`-only) needed no corresponding routine update, since none of it touches the routine's fetch strategy, sourcing rules, or `shows.json` schema/validation.

> **Auto-merge policy: tried, reverted, 2026-09-17 → 2026-09-24.** A separate session applied a directive ("assume changes are correct and automatically merge") to the routine's actual stored prompt on 2026-09-17, removing its entire "When NOT to auto-merge" gate (the >15-shows, >30-day date-shift, price-outlier, and unverified-source checks) via `update_trigger` — the tool this interactive session didn't have handy at the time it first heard the directive, but another session clearly did. One week under that policy produced a concrete, user-visible failure: a 4-hour timezone bug shipped in Refinery show times (PR #91) and stayed live until the user reported it themselves — plus an unresolved same-time double-booking and two Gaillard Center shows sourced from web search alone (no real fetch at all) that merged with only a flag, unreviewed. **Reverted 2026-09-24** — the gate is back, its source list updated for everything learned in between (GC's new CAPTCHA block, the BeatGig/livemusiccharleston.com sources, Tin Roof's tockify feed), plus two new trip-wires directly targeting what slipped through: "sourced from web search alone" and "two shows that conflict with each other." See "2026-09-24" note just below for the review mechanism that replaces blind auto-merge.

> **New: a weekly plain-language review check-in, so the user never has to open GitHub.** A second Routine (`Review Show Notice routine PRs`, Thursdays 9am ET — a few hours after the data routine's own 3am ET run) fires into this same interactive session, checks for any PR the gate above left open, summarizes each one in plain language (what changed, why it wasn't auto-merged), and asks the user yes/no per PR before merging anything. This is the actual "simple way to review" the user asked for — review happens right here in chat, not in GitHub's PR UI. On-demand review ("check for pending shows to review") also works anytime, same as any other request in this conversation.

### Research conventions

- **Completeness check, every time:** when updating any venue's data — even for a single-field fix — fetch that venue's own full calendar/schedule page and list every date+artist shown. Cross-reference against every existing `shows.json` entry for that venue code; anything on the venue's page not already in the data is a miss, add it. Do this every time, since there's no persistent memory of past sessions to rely on unless it's written down here.
- **Cover all available dates, not just a near-term window.** If a venue's own calendar lists shows two or three months out, reconcile against all of them. (A September-only pass on Music Farm once missed three real shows sitting in October/November on the same already-fetched page.)
- **Source priority — venue's own site first, always.** Prefer the venue's own site over Songkick, Bandsintown, Ticketmaster search snippets, or any other aggregator, for both event completeness and price accuracy. Aggregators are a fallback only when a venue's own site is genuinely unreachable for automated fetching.
- **Price sourcing priority:** venue's own site price > official ticketing platform (Ticketmaster/Afton/WhollyTicket/AXS-where-co-primary/SeatGeek-where-official) direct page > nothing. Never resale/aggregator prices (StubHub, VividSeats, TickPick, SeatGeek-as-resale-elsewhere, Gametime, viagogo, Eventworld, etc.) — these run 2–3x face value and would corrupt the dataset. If only a resale price is reachable, report "no reliable face-value price found" and leave the show unpriced.
- **Advance vs. day-of price: always store the advance price** in `p` (see Data model above).
- **Never leave a show at "discovery-source" data quality.** Confirmed bug, now fixed as a standing rule: 13 Charleston Pour House shows had a Jambase link as their `u` field and `"see ticket link"` placeholders for `dr`/`sh`, with no opener, stage, or price — even though each had a real, fully detailed event page on charlestonpourhouse.com itself. After finding a show exists (by any means, aggregator included), the URL and full detail fields (`dr`, `sh`, `p`, `o`, `s`) must come from the venue's own individual event page if one exists. Known constraint: Pour House's own `/shows/` listing paginates, and pages beyond the first are blocked by robots.txt; when that happens, search for the specific show by name + venue to find its individual event page directly, rather than falling back to an aggregator. **Update 2026-09-13:** this per-event-page fallback was confirmed working well past the originally-documented "~6 weeks" pagination cap — a run that same day fetched individual event pages out to mid-December cleanly.
- **Pour House has multiple independent stages (added 2026-09-13):** Main Stage, Deck Stage, and an outdoor Back Lot Stage, each bookable on the same date independent of the others — finding one show for a given date at this venue is not a signal to stop looking for that date. Deck Stage shows are typically ticketed via `aftontickets.com` (a real, legitimate ticketing platform for this venue, not resale) rather than the venue's usual flow — treat an aftontickets.com link the same as any other direct venue ticket link. See "2026-09-13: Eric Lindell missing from Nov 5" under Incident history above for how this surfaced.
- **Non-music events — exclude confidently, don't include-and-flag.** An earlier "if ambiguous, include and flag" rule let ten non-music events into the data in one session (Retroland Market, Booty Bounce Charleston, Sploinky Rave: Charleston, Off Campus Night, Gasolina Halloween Party, Sexy Unique Podcast, "Let's F*cking Date with Serena Kerrigan," 90s Burlesque, Emo Night Brooklyn: Halloween Edition) before being caught and removed. Revised rule:
  - Treat these as non-music by default, exclude outright, no flag needed: DJ sets/dance parties/raves, themed nightlife/"18+ party" nights with no booked act and no live-music mention, podcast tapings/talk shows/dating-show-style events, markets/vendor events with no music mentioned, burlesque/variety revues with no live band as headliner, comedy/Broadway/pure-sports events with zero music component.
  - **Carve-out** (decided after the "Bulls, Bands, and Barrels" and "Biker Oyster Roast" cases): if an event's own description explicitly mentions live music as one component — even a hybrid event (rodeo, cornhole tournament, biker rally, oyster roast), even with no specific act named — include it, with a `flag` noting the hybrid format. The line is whether music is part of the pitch at all, not whether a specific artist is named. Signal phrases: "live music," "live entertainment," "and bands," etc., anywhere in the venue's own copy.
  - Signal words that still mean exclude (no live-music mention present): "DJ," "Dance Party," "Rave," "Market," "Podcast," a talk/game-show-style title, "Burlesque," "[Something] Night" with no artist name and no live-music mention, sports/rodeo events with no music mention at all.
- **Classical/orchestral music: excluded entirely, current and future, no exceptions.** Gaillard Center's CSO/Masterworks series (10 shows) was removed from `shows.json` in an earlier session; the previous hide-by-default/per-venue-reveal-toggle design was removed from the code entirely.
  - **Resolved precedent — "Jazz in the Park" / Pops series (decided 2026-09-10):** Riverfront Park hosts a recurring "Jazz in the Park" series explicitly billed as "an evening of smooth jazz" and produced by the North Charleston Pops. This created a genuine rule conflict: the hybrid-event carve-out (above) says explicit live-music language should trigger inclusion, but the classical/orchestral exclusion says Pops-produced programming is excluded with no exceptions. **Decision: exclude.** The classical/orchestral exclusion takes precedence over the hybrid carve-out whenever the presenting organization is a Pops/orchestral ensemble, regardless of how the event's own marketing describes the music genre. Treat this as settled going forward — don't re-litigate it per-instance for this or similarly Pops-produced series.
- **Treat single automated research passes as needing spot-check verification, not settled fact.** Concrete case (2026-09-09 session): an Advanced Research pass concluded Firefly Distillery's own page was a dead "dark" source — wrong; a direct fetch the same session found 4 real, current shows. The same pass also admitted its own AXS event counts drifted within the session (Music Farm moved from ~44 to ~37 between an early spot-check and the final report). Any claim sourced only from a research pass — not a direct fetch/curl test in the same session — should be marked unverified until spot-checked, especially before it's written into something that runs unattended.

### Per-venue fetch strategy

| Venue | Best URL to fetch | Status |
|---|---|---|
| Charleston Pour House | `charlestonpourhouse.com/shows/` | Full HTML, all fields inline (price, doors, show, opener, stage). Paginates — page 1 only goes ~6 weeks out; further pages blocked by robots.txt. Beyond page 1, need each show's own `charlestonpourhouse.com/event/...` URL, often not yet search-indexed if new — hardest venue to complete far out. AXS (`axs.com/venues/130583`, 28 events) is useful as a cross-check/gap-catcher but should not outrank the venue's own site. |
| Music Farm | `musicfarm.com/calendar/` | Full HTML, all fields inline including the venue's own "Buy Tickets"/"LOW TIX"/"Sold Out" status labels. No pagination issue found — one fetch covered ~2.5 months. AXS (`axs.com/venues/124602`) count fluctuated ~44→37 within one research session — treat any stored count as stale-prone; cross-check only. |
| The Royal American | `theroyalamerican.com/schedule` | Full HTML, one long page covering many months (May 2026–Jan 2027 in one fetch). All fields inline (doors, price, opener, Instagram links per act). Best-behaved venue site found so far. No AXS page exists for this venue; Bandsintown/Songkick only catch the handful of ticketed touring dates, not the near-nightly local shows — the venue's own site is decisively more complete. |
| The Windjammer | `the-windjammer.com/events/` | Full HTML, all fields inline. Caution: the day-of-week label and trailing date stamp on each card aren't always internally consistent — cross-check against existing data before trusting a new date from this page alone. AXS (`axs.com/venues/130171`, 28 events) is a cross-check; be aware Bandsintown has duplicate venue entries for this venue — don't conflate them. |
| Credit One Stadium | `creditonestadium.com/events/` | Full HTML but only date/time/ticket-link, no price. Confirmed still Ticketmaster/Seated as of 2026-09-09 (an earlier research-pass claim of a May 2026 SeatGeek exclusivity switch was wrong — see below). AXS (`axs.com/venues/120907`) is a secondary cross-check only. |
| Firefly Distillery | `fireflydistillery.com/upcoming-shows/` | **Changed 2026-09-17: now CAPTCHA-blocked.** Was confirmed working as of 2026-09-09 (full HTML, real show names/dates) — that's no longer true. Every request (tested with multiple header/cookie variations, including a fake "age already verified" cookie in case it was actually an age gate, not a bot-check) now gets an `sg-captcha: challenge` response header from SiteGround's own bot-protection product, redirecting to `/.well-known/sgcaptcha/` with an empty page — confirmed via direct response headers, not inferred. This is a genuine anti-automation wall, not the site's separate age-verification prompt (a real browser passes the captcha challenge invisibly before ever seeing that prompt). Not fixable by this routine's plain-HTTP fetch method. AXS (`axs.com/venues/129043`) and Bandsintown both carried current secondary listings as of 2026-09-09 but weren't re-tested 2026-09-17. |
| North Charleston Coliseum & PAC | `northcharlestoncoliseumpac.com/events` | Full HTML list, but no times/price on the list itself. Individual event pages are directly fetchable even without being search-indexed (e.g. `/events/detail/travis-tritt-2` worked on the first try) — the one venue where guessing the individual-page URL pattern from the list page's own links worked. Lists many non-music events (comedy, Broadway, sports, POPS orchestral, Disney on Ice) mixed in — needs filtering every time. AXS is a genuine co-primary here (the venue's own FAQ names AXS alongside Ticketmaster for select events; two AXS pages exist, Coliseum and PAC halls separately). Individual venue event-detail pages sometimes carry a "starting at $X" line even when Ticketmaster is blocked for price — check those before assuming Ticketmaster is the only source. |
| Charleston Music Hall | Ticketmaster only, price-blocked | Ticketmaster's bot detection prevents price capture; the Music Hall's own site doesn't display prices either. AXS tested twice on 2026-09-09 with conflicting results depending on client — see "Secondary-source bot detection is client-dependent" below. Currently a documented limitation, not a usable workaround, for the unattended routine. |
| New Realm Brewing | No working primary | See "New Realm Brewing: dual failure mode" below. Do not use `newrealmbrewing.com/charleston/live-music-events/` as a fetch target. |
| The Refinery | `therefinerychs.com` | **Added 2026-09-17** (was previously tracked in `shows.json` with metadata only, not part of this table). Wix site — the concert listing isn't in the page's Wix Events JSON data blob (that only holds an unrelated market/fitness calendar); band names, dates ("Fri, Sep 25" — no year, no time) render as plain visible text in a hand-built content block. Confirmed 100% date-accuracy on all 9 upcoming shows checked against existing data. Openers sometimes only appear in the full event title text (e.g. "Judah & The Lion with Special Guests Breakfield and Band of Silver") even when the existing `o` field is empty — check the full title text, not just the bold band name, before assuming no opener. Ticket links are `ticketweb.com`, found via each show's own "Buy Tickets" href. No show times on this page at all — `charlestonlivemusic.com/venue/the-refinery/` (see below) has clean per-event times via schema.org JSON-LD (`<script type="application/ld+json">`, `startDate` field) and is a good second-source for that. |

*(Note: this table predates the 8 venues added 2026-09-12 — Tattooed Moose ×2, Uptown Social, Tommy Condon's, The Dinghy, Rita's Seaside Grille, Commonhouse Aleworks, Slightly North of Broad — and today's removal of 5 of those 8 (Tommy Condon's, The Dinghy, Rita's, both Tattooed Moose locations, per explicit user request 2026-09-17). See "New venues added 2026-09-12" and "Venues removed 2026-09-17" below for those two changes; `shows.json`'s own `venues` object is the current source of truth for which venue codes are actually tracked.)*
| Charleston Tin Roof | **Changed 2026-09-17: real working source found.** `webcal://tockify.com/api/feeds/ics/tinroofschedule` (fetch as `https://tockify.com/api/feeds/ics/tinroofschedule`) — the venue's own calendar-subscribe link, supplied by the user. Standard iCal/ICS format, no bot-protection (calendar-subscribe feeds are meant for automated consumption). 538 total `VEVENT`s, ~500 upcoming, but **484 of those are 4 recurring non-music filler entries** (a weekly food pop-up, a weekly F&B night, a weekly happy hour, a recurring comedy open mic) mechanically extended out to 2028 — filter these out (dedupe by `SUMMARY` text; anything repeating >2 times in the feed is filler, not a real distinct booking). The remaining ~16 are genuine one-off bookings. Door/show times and prices are embedded directly in each event's own `SUMMARY` text (e.g. `"Door @ 7. Show @ 8. $10 21+"`) with no AM/PM marker — always PM for this venue (bar, evening shows only), confirmed by cross-checking against the feed's own UTC `DTSTART` for entries that lack embedded door/show text. `DTSTART` itself is UTC and needs conversion to `America/New_York` (the feed's own `X-WR-TIMEZONE`). See `git log` for PR #82's full writeup of the first pull. Old sources (own site robots-blocked, Bandsintown bot-blocked, `charlestonlivemusic.com` empty for this venue) remain dead but are no longer needed. |

### The core technical constraint (why some venues are hard)

The `web_fetch`-style tool can only open a URL that has already appeared in a search result, a prior fetch, or something the user typed directly — it doesn't matter if the page exists and is publicly reachable; if it hasn't surfaced in search yet (common for recently-published event pages) or robots.txt disallows the path, it's a dead end regardless of how the query is phrased. Reformulating the search rarely helps once this is confirmed. Two reliable ways around it:
1. The user pastes the direct URL — a batch of venue homepage URLs is far more effective than searching for each show individually.
2. A base listing page (Pour House's `/shows/`, Music Farm's `/calendar/`) is usually reachable even when individual event pages or later pagination aren't — always try the venue's main calendar page first, in full, before hunting for anything more specific.

Browser automation would remove this constraint entirely, since it navigates live rather than requiring a prior search hit — but requires Chrome open with the extension connected, which hasn't been set up in past sessions.

### Cloud environment network-egress allowlist (routine-specific, resolved 2026-09-09/10)

Distinct from the `web_fetch` search-index constraint above — this applies specifically to the Claude Code Routine's cloud sandbox, not interactive chat sessions.

**What was wrong:** the routine reported all 7 venue domains as `EGRESS_BLOCKED` on a scheduled run, despite the cloud environment's Custom network allowlist visibly containing them. Direct curl tests inside a manual session on the same environment found the allowlist only contained wildcard-form entries (`*.charlestonpourhouse.com`, `*.musicfarm.com`, etc.) — no bare-apex entries. A wildcard like `*.example.com` conventionally matches subdomains only, not the bare domain. Since venue sites' `www.` versions 301-redirect to the bare apex, and the routine's own fetch strategy targets bare-apex URLs directly, every real request hit exactly the unmatched form.

**Confirmed by direct test:** `charlestonpourhouse.com` and `musicfarm.com` (bare) were blocked at the proxy's CONNECT stage before reaching the real server; their `www.` variants reached the server and returned a 301 back to the bare domain. A control request to `example.com` (never intended to be allowed) was also blocked at the same stage, confirming the allowlist was being enforced at all.

**Fix:** the allowed-domains list needs both the bare-apex and wildcard form for every domain the routine needs, including secondary sources (AXS, Eventbrite, SeatGeek, Bandsintown). Applied and confirmed working: the routine's first real run after the fix fetched 20 pages and produced a PR with real corrections, no `EGRESS_BLOCKED` failures.

**Verification status:** only Pour House and Music Farm domains were curl-tested end-to-end to confirm the original diagnosis. The rest of the venue domains are believed to need (and now have) the same fix by pattern, but individual confirmation across all 11 venues is still an open item beyond what showed up in the first post-fix PR (Pour House, Windjammer, Music Farm, NCC all appear to have worked based on that PR's contents; Royal American, Credit One, Firefly, and Charleston Music Hall's own site weren't individually confirmed one way or the other).

**Domains added 2026-09-17 (interactive session, both bare-apex and wildcard form each):** `tockify.com` (Tin Roof's real calendar feed — see "Tin Roof" below), `beatgig.com` + `backend.beatgig.com` (the real API host is the `backend.` subdomain, not the bare domain — see "beatgig.com investigated" below), `therefinerychs.com` (Refinery), `charlestonjazz.com` (Charleston Jazz Festival), `northcharleston.org` (Riverfront Park's city-department info page — turned out to have no event content, but the domain itself is now reachable), `ticketmaster.com` (reachable now, but still returns its own site-side 403 bot-block on request — this did NOT fix Ticketmaster access, just confirmed the block is genuinely site-side, not proxy-side).

### Secondary-source bot detection is client-dependent (discovered 2026-09-09)

A third, separate pattern from testing AXS, Bandsintown, and Eventbrite directly from inside the routine's sandbox: AXS (for Charleston Music Hall) and Bandsintown (for both New Realm Brewing and Charleston Tin Roof) all returned genuine 403s from those sites' own servers when requested via curl from inside the routine's sandbox — confirmed via the proxy's own status log, which showed the tunnel succeeding and the block originating from the target site, not the proxy. The same URLs, requested via a different, interactive fetch tool used earlier in the project, returned clean, complete data with no block at all.

**What this means:** not a network-access or allowlist problem, and not fixable by editing the domain list. It's the sites' own bot-detection distinguishing between the routine's fetch client (resembling plain curl, presumably lacking browser-like headers/TLS fingerprint) and whatever client the interactive tool uses. Plausible inference, not confirmed against any vendor's documentation: ticketing/event platforms commonly run WAF-style bot protection (Akamai, PerimeterX, Cloudflare, or similar) that fingerprints requests below a "looks like a browser" threshold — consistent with what's observed, but the specific vendor for AXS or Bandsintown hasn't been confirmed.

**Practical implication:** AXS-for-CMH-pricing, Bandsintown-for-New-Realm, and Bandsintown-for-Tin-Roof are downgraded from "usable secondary source" to "confirmed to have real data, but not presently reachable by the routine's actual fetch mechanism." This differs from Firefly's page being genuinely dark, or New Realm's own captcha wall — the data exists and a fetch *can* reach it, just not from this client. Fixing it would need either the routine's fetch tool sending more browser-like headers (unconfirmed whether configurable) or a different fetch approach (e.g. browser automation).

### New Realm Brewing: dual failure mode (captcha AND JS-rendering, depending on client)

- **Via curl inside the routine's sandbox:** the events page returned an HTTP 202 with a SiteGround `sgcaptcha` bot-challenge response — confirmed via full verbose output showing the challenge redirect body and headers (`sg-captcha`, `x-robots-tag: noindex`).
- **Via a different fetch tool, same session:** the identical URL returned a clean 200 with no captcha — but also no actual event data. The page content is navigation, a 6-location switcher, and marketing copy; the calendar is very likely rendered client-side via an embedded widget (`app.teamdexter.com`, visible in a "Book your event!" iframe embed) that a static HTTP fetch won't execute.

**Conclusion:** regardless of which failure mode the routine's environment hits on a given run, no usable show data comes back from this URL either way — don't add it back to the routine's fetch strategy.

**Fallback sources (status as of 2026-09-09, after direct curl testing from inside the sandbox):**
- **Bandsintown** — genuine 403 from Bandsintown's own servers (confirmed via proxy log), matching what the actual scheduled routine run reported the same day — a corroborated finding, not a single test. Not usable inside the unattended routine unless a header/client change resolves the bot-detection block.
- **Eventbrite** — the curl test got a clean 200, no block. This conflicts with the same day's actual routine run, which described Eventbrite as unreachable alongside Bandsintown — an unresolved inconsistency (possibly imprecise routine summary language, possibly a genuine request difference). Before relying on Eventbrite in the routine, verify the actual response body contains real event data and isn't a JS-rendering shell (the research pass flagged this page as JS-rendered separately, which would make a 200 status misleading).
- **Unexplored lead:** the `app.teamdexter.com` embed might expose its own API or embeddable calendar endpoint — nobody has checked this yet.

### Secondary source research (AXS, Eventbrite, Bandsintown, Songkick, Dice, SeatGeek)

An Advanced Research task (2026-09-09) audited secondary event-data sources across all 11 tracked venues; full findings are in the delivered research artifact, summarized here with the caveat that several specific claims from this pass need direct-fetch verification before being trusted unattended (the Firefly "dark page" claim from this same pass was already disproven directly).

**Headline conclusions:**
- **AXS.com** has active, current pages for 8 of 11 venues (Music Farm, Charleston Music Hall, Windjammer, Firefly, Credit One, both NCC halls, Pour House) and is the strongest secondary source for this venue set.
- **Bandsintown** has some coverage for nearly every venue, reliable for existence/dates — never price. Watch for duplicate venue pages (Windjammer and Pour House both have more than one Bandsintown entity) — pin the specific venue ID rather than trusting the first search result.
- **Dice.fm has zero coverage of any tracked venue** — not worth checking going forward.
- **Songkick** is thin and fragments some venues (Pour House especially) across duplicate/stage-specific pages — demote to a tertiary fallback.
- **Eventbrite** only matters for New Realm and Charleston Pour House (organizer pages).

**Sourcing-priority nuance — AXS is not one flat tier:**
- At **Charleston Music Hall and North Charleston Coliseum & PAC**, AXS is a genuine co-primary/official seller (NCC's own FAQ names AXS alongside Ticketmaster) — belongs in the "official ticketing platform" tier for these two venues specifically.
- At **Music Farm, Windjammer, and Pour House**, AXS is a cross-check against an already-complete venue site — should not outrank the venue's own page.
- **Charleston Music Hall specifically:** AXS may show face-value pricing where Ticketmaster is bot-blocked for price capture. This remains the single highest-value *unverified* claim from the research pass — confirm with a direct fetch of a specific AXS/CMH event page before relying on it; as of 2026-09-09 AXS itself returned a 403 from the routine's sandbox client (see "Secondary-source bot detection" above), so this is currently blocked regardless of whether the underlying claim is true.
- **Credit One Stadium:** a research-pass claim that SeatGeek became the venue's exclusive official ticketer (May 2026) was checked directly on 2026-09-09 and found false — the venue was still on Ticketmaster/Seated. SeatGeek remains excluded everywhere for this venue set; no action needed.

**Known limitations of the research pass itself:** event counts are point-in-time and had already visibly drifted within the same session (Music Farm's AXS count moved ~44→37 between an early spot-check and the final report). AXS pages surface a resale "Marketplace" option alongside face-value tickets — capture only the primary/advance price line if AXS is used for pricing. Treat every specific claim in the report as needing a spot-check before being trusted unattended, per the Firefly precedent above.

### Riverfront Park festival sub-sources (added 2026-09-10)

Riverfront Park (`RFP`) hosts several independently-branded festivals with their own official sites, distinct from the general venue page:
- **Low Tide Festival** and **High Tide Festival** both live on the same official site, `hightidefestival.com` (a Shopify storefront) — Low Tide's own event/pass page is `hightidefestival.com/products/low-tide-2026-pass`-style URLs; check the homepage banner link if the exact slug is unknown. Confirmed reliable, venue/organizer-official tier.
- **Riverfront Revival** — official ticketing is Frontgate Tickets (`riverfrontrevival.frontgatetickets.com`). The lineup announcement itself is best-confirmed via corroborating local news coverage (WCIV/ABC News 4, CountryNow, etc. all independently reported the same 2026 lineup) rather than a single self-contained official press page — treat multi-outlet corroboration as a reasonable substitute for a single official source when one isn't readily fetchable, but note it explicitly rather than presenting it as venue-tier-sourced.
- **The Riverfront Park venue's own Instagram, `@theriverfrontpark`**, posts a monthly "What's Happening" flyer graphic with dates/times for its self-run community events (Jazz in the Park, Taco & Margarita Festival, National Night Out, etc.) — useful for the venue's own small/recurring events, though it's an image graphic, not fetchable text, so it requires the user to screenshot/upload it rather than a direct `web_fetch`.
- Full artist-lineup graphics posted on festival sites/socials are often images, not text — `web_fetch` only returns page text/metadata and cannot read text baked into an image. When a lineup is only available as a flyer graphic, ask the user to upload it rather than guessing from secondary text sources.

### Data-extraction techniques that worked

- Many venues print both advance and day-of prices directly in plain text on the listing page itself (e.g. Pour House's "PRICE: $15 – $20," the Windjammer's cornhole event's "$15 in advance | $20 at the door") — more reliable than fetching a separate ticketing platform; check the venue's own descriptive text first.
- Status labels are often already in the venue's own HTML: "LOW TIX," "Sold Out," "On Sale Soon" appear as plain text/link labels on Music Farm and Windjammer listings — no separate lookup needed, just read the label next to "Buy Tickets."
- Individual event pages sometimes work even when list pagination doesn't (NCC is the clean example) — worth trying a constructed/found individual page URL directly even after the list hits a wall.
- AXS venue pages are fetchable directly and expose an "N events" count plus a full list (`axs.com/venues/{id}/{slug}-tickets`) — always trust a direct fetch over an AXS search-result snippet (a CMH search snippet once returned "no events scheduled" while the same page, fetched directly, showed 82 events). AXS pages also surface a resale "Marketplace" option — capture only the primary/advance price line, never Marketplace, per the no-resale rule.

### Non-music exclusion — quick reference of what got filtered out

DJ nights/dance parties/raves, themed party nights with no booked act, podcast tapings, talk shows, dating/game shows, markets/vendor events, burlesque/variety revues, comedy specials, Broadway/theatre productions, sports (hockey, tennis), ice shows, and orchestral "Pops" tribute concerts (same reasoning as the classical exclusion). Signal words: DJ, Dance Party, Rave, Market, Podcast, Burlesque, POPS, or a real person's name with "Comedy"/"Tour" and no band. Applies equally to AXS listings, which mix in the same categories (confirmed at both Music Farm and Charleston Music Hall — e.g. "Kawaii Rave," "I Love RnB Party," "Serial Killers with Dr. Scott Bonn" lecture).

### Standing known issues (don't re-report every run unless something changes)

- **Charleston Music Hall & Music Farm face-value prices:** Ticketmaster blocks automated price capture; only resale prices were reachable for most CMH/MF shows and, per the sourcing rule, were not entered into the data. AXS shows the data via some clients but is bot-blocked via the routine's actual fetch mechanism — documented limitation, not something to re-verify every run (see "Secondary-source bot detection" above). Music Farm's own site + Ticketmaster remain the priority there.
- **The Yacht Club (Windjammer, Sep 12):** only a recurring-series price pattern was found ($15 adv/$20 DOS from a 2025 instance), not confirmed for the 2026 date — deliberately left unpriced.
- **Credit One Stadium ticketing platform:** resolved, false alarm — confirmed still Ticketmaster/Seated on 2026-09-09; no action needed.
- **New Realm Brewing coverage:** primary source unusable (dual failure mode). Bandsintown bot-blocked. Eventbrite tested clean via curl but conflicts with the same-day routine run reporting it unreachable — needs the routine's raw output checked to resolve the discrepancy, plus a check of whether the response body has real content. `teamdexter.com` embed lead still unexplored.
- **Charleston Tin Roof coverage:** Bandsintown fallback (previously treated as reliably working) is now confirmed bot-blocked from inside the routine's sandbox — same open status as CMH/New Realm.
- **Gaillard Center:** out of scope entirely (classical/orchestral programming).
- **Riverfront Revival 2026 Friday/Saturday artist split:** the full 17-act lineup is confirmed (multi-outlet corroboration, 2026-09-10), but the per-day breakdown is only sourced from an AXS listing covering 10 of the 17 acts (Fri: Gavin Adcock, BigXthaPlug, Charles Wesley Godwin; Sat: Darius Rucker, Old Dominion, Nelly, Chase Matthew, Trombone Shorty & Orleans Avenue, Austin Williams, Karley Scott Collins) — the other 7 acts aren't day-assigned anywhere found. `shows.json` stores this as one two-day entry (`d`/`e`) with the full lineup in `o`, flagged as partially-unverified per-day. Worth re-checking once the festival publishes its own daily set-time page (High Water's `daily-lineups` page is the model for what to look for).

**Windjammer date reliability:** this venue's own site has shown real, repeated date/stage mismatches against previously-stored data (10 separate corrections in one 2026-09-09 run) — not a one-off scrape error. Example: Swimming Pool Q's moved from Sep 17 to Sep 13; Winyah's two nights moved from Sep 18/19 to Sep 17/18, both fixed by trusting the venue's own site over existing data. Standing rule: when a venue's own site conflicts with what's already in `shows.json`, the venue's site wins — correct the data rather than flagging it as an open question, unless the conflict is itself ambiguous (e.g., two different venue-run pages disagree with each other). Any future Windjammer date/stage change is worth an independent re-fetch/verification (date badge + day-of-week + URL slug) before trusting it.

## Known open data issues

~~**12 more Charleston Pour House shows still have the Jambase-link/placeholder-data problem**~~ **Resolved 2026-09-13, PR #73** — all 12 originally flagged (LaMP, The Greyboy Allstars, Porch Light, Neal Francis, Tommy Prine, Satsang, SunSquabi, TAUK, The Fretliners, Deer Tick, Magnolia Boulevard, moe.phrey's) backfilled from each show's own charlestonpourhouse.com event page, plus 3 more found in the same state (Doom Gong, Bearly Dead, John Cowan Trio) and 17 further new shows discovered in the same pass. "moe.phrey's" confirmed as the venue's actual name for the collective, not a mistranscription. See "2026-09-13: Eric Lindell missing from Nov 5" under Incident history above for the fuller story (this same PR is also where that show was added).

## Outstanding action items

**Resolved, no further action needed:**
- Network egress allowlist fix — confirmed working by observation (the routine's first real run after the fix fetched 20 pages, produced a PR with real corrections, no `EGRESS_BLOCKED` failures).
- Credit One Stadium ticketing platform — confirmed still Ticketmaster/Seated, not SeatGeek; research-pass claim was wrong, routine doc corrected.
- worker.js corruption (PRs #3/#4) — rebuilt from known-good commit in PR #5; sanity-check CI step added to prevent recurrence.
- The 8 code-review findings from the 2026-09-10 project review (see Incident history above).
- **Low Tide Festival (Sept 12, 2026) and Riverfront Revival (Oct 9–10, 2026)** — both confirmed via official/multi-outlet sources and added to `shows.json` on 2026-09-10 (see "Riverfront Park festival sub-sources" above for how each was sourced).
- **"Jazz in the Park" / Pops-vs-hybrid-carve-out rule conflict** — resolved 2026-09-10; decision (exclude) and reasoning documented in the classical/orchestral exclusion section above.
- **Frontend UX session, 2026-09-10 (PRs #10–#17)** — favorite-artist matching against openers, two storage-reliability fixes, a mobile venue-page overflow fix, a header utility-row reorg with new jump links, header sizing, a star-to-favorite-artist flow (including a per-artist picker for multi-artist/festival shows), and a full native-`alert()`/`confirm()` → custom-modal sweep. See "UI dialogs," "Header layout & mobile responsiveness," and "My Shows / Favorite Artists model" above for details.
- **Header/navigation redesign, 2026-09-10 (PR #19)** — sticky title banner, sticky per-section headers, "Full List" renamed to "All Shows" with its own section, chained "Back to top"/"next section" links on 5 sections, and the PR #13/#16/#17 utility-row/two-column nav fully replaced by a single-row nav-pill layout. See "Header layout & mobile responsiveness" above.
- **Redesign follow-ups, 2026-09-10 (PRs #21–#24)** — pill-row divider, sticky venue names, tighter mobile nav pills, sticky date headers extended to all four show lists (My Shows/Added This Week joining All Shows/venue pages, via a new shared `groupByDayHTML()` helper), `flatDate` removed as dead code, and a sticky "← Back" link on venue pages. See "Header layout & mobile responsiveness" above.
- **Branch cleanup, 2026-09-10** — "Automatically delete head branches" confirmed enabled and working (every PR from #18 on auto-deleted its branch on merge); the 13 legacy pre-setting branches were deleted manually. `main` is now the repo's only branch. See "Repo notes" below.
- **Domain migration, 2026-09-11 (PRs #26–#31)** — moved off `gigalertchs.com` onto `chs.shownotice.com`/`api.chs.shownotice.com`, old domain kept alive as a working 301 redirect, stale `*.workers.dev` Worker route removed from the dashboard. See "Domain migration" above.
- **Admin analytics panel build-out, 2026-09-11 (PRs #40–#49)** — full week/month/year/all-time stats across subscribers, accounts, digest engagement, favorited artists/venues, starred shows, and suggestions, plus Cloudflare zone traffic/threats/cache/error-rate/country data, redesigned layout. See "Admin analytics panel & Cloudflare integration" above.
- **Workers Logs + Traces observability declared in `wrangler.toml`, 2026-09-11 (PR #50).**
- **Full-project debug/security/stability audit, 2026-09-11 (PR #51)** — 6 findings (1 data race, 1 lockout bug, 1 dead-code/unfinished feature, 1 fragile config, 1 performance issue, 1 data-consistency mismatch), all fixed; manual security sweep found nothing further. See "2026-09-11: full-project audit" under Incident history above.
- **Account model + Contact Us overhaul, 2026-09-12 (PRs #66–#68)** — combined About & Contact footer panel with a public Contact Us form (no visible admin email on the page anymore), explicit subscribe/unsubscribe semantics tied to the sign-in checkbox (`subscribeEmail()`/`markUnsubscribed()` replacing an implicit `ensureSubscribed()`), admin panel updated to show Contact Us messages, cron schedule changes (scraper routine → 3am ET Thursdays, digest → 1pm ET Thursdays), the "No spam, ever." note placed next to the sign-in link, and the past-show cutoff fixed to drop shows same-day instead of with a 1-day grace period. See "Account model: sign-in, subscribe/unsubscribe & Contact Us" above.
- **Nav/layout redesign, 2026-09-12 (PRs #69–#70)** — top nav pills reordered to All Shows/My Shows/Favorite Artists and centered, Suggest a Venue pill removed from the top nav, venue/festival pill directory moved to the page bottom, sign-in relocated into My Shows (and underlined), Last Updated moved to the footer, and the All Shows venue filter rebuilt as a multi-select checkbox panel styled like the section jump-links. Venue pages now show "Website" instead of a raw URL and pick up that venue's own accent color instead of uniform amber. See "Nav/layout redesign, 2026-09-12" above.

**Not yet resolved:**
1. **AXS/Bandsintown bot detection** — AXS (for CMH pricing) and Bandsintown (for New Realm and Tin Roof) are confirmed reachable by some clients but bot-blocked by whatever fetch mechanism the routine actually uses. Not fixable via the domain allowlist. Worth investigating whether the routine's fetch tool can send browser-like headers, or whether this needs a different approach (e.g. browser automation).
2. **Eventbrite/New Realm discrepancy** — a direct curl test got a clean 200, but the same-day production routine run reported it unreachable. Needs the routine's raw output (not just its summary) checked, plus a check of whether the response body has real content or is JS-rendered.
3. **Spot-check remaining venue domains** against the corrected allowlist beyond what showed up in the first PR (Pour House, Windjammer, Music Farm, NCC appear to have worked) — Royal American, Credit One, Firefly, and Charleston Music Hall's own site weren't individually confirmed in the first run's summary.
4. **Decide whether to pursue the `teamdexter.com` embed lead** for New Realm — the one lead not yet tested at all.
5. **Confirm whether AXS shows face-value price for Charleston Music Hall** once the bot-detection block is resolved by some means (item 1) — try again from a different network context/client if possible.
6. **Turn on required PR review for `main`**, if not already in place — the CI sanity-check step catches the specific corruption class that happened once (duplicate function declarations) but doesn't replace human review for anything else.
7. **Riverfront Revival Friday/Saturday day-split** — currently sourced from AXS and only covers 10 of 17 acts; re-check once the festival publishes its own daily set-time page (see "Standing known issues" above).
8. **No automated test suite** (see "Deploy process" above) — all validation this session was manual. Worth a small smoke-test suite for the Worker's request handlers if the project keeps growing.
9. **Admin session-expiry UX** — a 401 now recovers gracefully (PR #51) but there's no advance warning before a session expires; worth a "session expiring soon" notice if the owner finds themselves logged out mid-task often. Not actioned — a suggestion, not a confirmed problem.
10. **Suggestion-form rate limit is per-KV-key, not atomic** — same class of best-effort limitation as the per-IP sign-in rate limit (see Security posture above); Turnstile is the tighter control in front of it. Low risk, not worth added complexity (e.g. Durable Objects) unless abuse is actually observed.
11. **8 new venues added to `shows.json` 2026-09-12, no shows scraped for any of them yet** — metadata only (name/site/type/color/address); see "New venues added 2026-09-12" below for the routine to pick up on its next run.
12. **Charleston Tin Roof still has no working automated source** — `charlestonlivemusic.com` was investigated 2026-09-12 as a candidate and confirmed real/reachable, but currently has zero listed events for this venue; not a fix. Worth an occasional re-check (not every run) in case the venue's listing there becomes populated. See "charlestonlivemusic.com investigated 2026-09-12" below.
13. **The venue/festival pill directory at the bottom of the page may be removed entirely** — the user's own framing when asking for it to move there was "might remove that... we'll see" (2026-09-12, see "Nav/layout redesign" above). Don't be surprised if a future request removes it outright rather than repositions it again; it isn't a settled part of the design.
14. **Suggest a Venue is now nav-orphaned** — reachable only by scrolling since its top-nav pill and all cross-links to it were removed in the 2026-09-12 nav redesign (see above), while Contact Us (new the same day) covers some of the same "something's missing/wrong" use case through a more prominent, always-reachable form. Worth asking the user whether Suggest a Venue should be merged into Contact Us, restored to some nav path, or left as-is — not decided, just left in its current de-emphasized state by default.
15. **The cut-off "need to work on presentation for the [...]" instruction (2026-09-12) was only partially addressed.** The user's message ended mid-sentence; a same-turn follow-up clarified "...entire site... need just a little more energy. Not too much tho," which was treated as authorizing only two small, low-risk items (a per-venue accent color on venue pages, and a slightly richer title-banner glow — see "Nav/layout redesign" above) rather than a broader visual pass. If the user actually wants more sitewide visual "energy," that hasn't been scoped or built yet — ask for specifics rather than assuming either of those two changes fully covers it.

### New venues added 2026-09-12 (metadata only — no shows scraped yet); 5 of the 8 removed 2026-09-17

User-requested addition, added directly to `shows.json`'s `venues` object from this interactive session rather than by the scraping routine. Each venue's own site is set as the `site` field (used as the primary source); `livemusiccharleston.com` was given as a secondary/aggregator source for every one of these and hasn't been individually vetted the way the "Secondary source research" section above vetted AXS/Bandsintown/etc. — worth the same kind of spot-check (does it actually list current, accurate shows for these specific venues) before leaning on it if the venue's own page turns out thin or unreliable.

- ~~**The Tattooed Moose — two locations, tracked separately**~~ **Removed 2026-09-17, explicit user request.** Was `TMPC` (Park Circle) and `TMJI` (Johns Island), same official site with no dedicated live-music page for either location — both venues and all their shows deleted from `shows.json`.
- **`US` — Uptown Social**, 587 King St, Charleston, SC 29403. Site: https://www.uptownsocialchs.com/entertainment/. Secondary source: https://livemusiccharleston.com/venue/uptownsocial. **Still tracked.**
- ~~**`TC` — Tommy Condon's**~~ **Removed 2026-09-17, explicit user request.**
- ~~**`DIN` — The Dinghy**~~ **Removed 2026-09-17, explicit user request.**
- ~~**`RSG` — Rita's Seaside Grille**~~ **Removed 2026-09-17, explicit user request.**
- **`CHA` — Commonhouse Aleworks**, 4831 O'Hear Ave, North Charleston, SC 29405. Site: https://commonhousealeworks.com/events/. Secondary source: https://livemusiccharleston.com/venue/commonhousealeworks. **Still tracked** — own site turned out to be a JS widget (see "beatgig.com investigated" below), `livemusiccharleston.com` is the only usable source.
- **`SNOB` — Slightly North of Broad**, 192 E Bay St, Charleston, SC 29401. Site: https://snobcharleston.com/charleston-french-quarter-slightly-north-of-broad-live-music. Secondary source: https://livemusiccharleston.com/venue/slightlynorthbroadrestaurant. **Still tracked**, same own-site-is-a-JS-widget situation as CHA.

Addresses/sites above came from a web search from this interactive session (not a direct fetch of each venue's own page, which this session's network policy blocks) — treat as reasonably reliable for something as stable as a street address, but worth the routine's own normal direct-fetch verification the first time it actually scrapes each of these, same as any other venue. None of the non-music exclusion rules, pricing conventions, or the no-resale rule have been checked against these specific venues yet — apply the same standing rules documented elsewhere in this file when scraping them for the first time.

**Note for the routine's own fetch table (not yet updated there — this session can't reach it):** remove `TC`/`DIN`/`RSG`/`TMPC`/`TMJI` from the per-venue fetch table entirely, or the routine will likely re-scrape and re-add them on its next run, undoing this removal.

### livemusiccharleston.com is a real API-backed source, not a dead JS shell (found 2026-09-12)

A PR opened earlier the same day (#62) had tried and failed to get anything from `livemusiccharleston.com` for the 8 venues above — a plain fetch returns an empty client-rendered shell (`<div id="root"></div>`), and a headless-Chromium attempt failed even earlier, with `ERR_CONNECTION_RESET` during page navigation. A follow-up run the same day root-caused both:

- **The headless-Chromium failure is an environment limitation, not a site-specific block.** Confirmed by pointing the same Playwright/Chromium setup at a known-good, curl-reachable page (`tattooedmoose.com`, verified `HTTP 200` via plain fetch moments earlier) — it failed identically (`ERR_CONNECTION_RESET`), with or without an explicit `proxy` option passed to `chromium.launch()`. The proxy's own status endpoint (`$HTTPS_PROXY/__agentproxy/status`, `recentRelayFailures`) showed the CONNECT tunnel to both hosts establishing fine, then closing mid-exchange (`ws_closed_mid_exchange`, code 1006) after ~6s having sent a TLS ClientHello and received only ~39 bytes back — consistent with the proxy's WebSocket-based relay for a real browser's connections not completing a TLS handshake through this tunnel at all, for any destination. **Headless-browser navigation cannot be used through this session's proxy, full stop — don't re-attempt it as a per-site workaround.** Plain `curl`/fetch-style requests through the proxy work completely normally (confirmed throughout this same session) and remain the only viable fetch method here.
- **The "empty shell" is genuinely just server-side-rendering-free React (Vite bundle), but the data behind it is a plain same-origin REST API, reachable with an ordinary plain fetch — no browser needed.** Fetching the page's own JS bundle (`/assets/index-*.js`, linked from the `<head>`) and grepping it for string literals turned up a set of relative API paths the app calls client-side: `/api/venues`, `/api/venues/names`, `/api/events/global`, plus a few unrelated to show data (`/api/blog`, `/api/chat`, `/api/market`, `/api/subscribe`). All are same-origin under `livemusiccharleston.com` itself — **there is no separate API subdomain to add to the allowlist**, the site was already fully reachable. `GET /api/venues/names` returns a `{slug: displayName}` map for every venue on the platform (confirmed slugs for all 8 onboarding venues: `thetattooedmoose` = Park Circle/`TMPC` [4845 Chateau Ave], `tattooedmoosejohnsislandsc` = Johns Island/`TMJI` [3328 Maybank Hwy], `uptownsocial`/`US`, `tommycondonsirishpub`/`TC`, `thedinghy`/`DIN`, `ritasseasidegrille`/`RSG`, `commonhousealeworks`/`CHA`, `slightlynorthbroadrestaurant`/`SNOB` — addresses cross-checked against `shows.json`'s own `venues` object and matched exactly). `GET /api/events/global` returns every booked event platform-wide (~620 events, no auth, no pagination needed) as structured JSON: `start`/`end` (UTC ISO timestamps), `venue.slug`, `artist.name`/`artist.configuration` (`Solo`/`Duo`/`Trio`/`Full Band`/`DJ`), `artist.genres`, `category`, `stage`, `description`, and empty `ticket_link*` fields for every event checked (this platform's events appear to be free/no-cover bar bookings — no price data exists for these venues via this source, consistent with expectations for pub/bar gigs; don't invent a price).
- **This is the actual underlying platform behind the white-labeled "Live Music Charleston" cross-check site** — the JS bundle references `dusk.fm`/`dusk.report` (booking-artist signup links), so this looks like a shared booking/calendar SaaS ("Dusk") that a number of small Charleston venues use, white-labeled as both `livemusiccharleston.com` and presumably other city-specific domains. Worth knowing if a similar "empty JS shell, venue site has nothing" situation comes up for a future venue — check for the same same-origin `/api/...` pattern before concluding a source is dead.
- **Filtering this feed still needs the same non-music/DJ rules applied by hand** — the API's own `is_live_music` boolean is not trustworthy for this (spot-checked: it returned `true` on a pure-DJ event). Use `artist.configuration` instead: `Solo`/`Duo`/`Trio`/`Full Band` = real live act, include; `DJ` = exclude per the standing DJ rule. A `configuration: null` event needs its `description` read individually (same as any other ambiguous case) — concretely, this session's pass through it excluded trivia nights, a beer/lager promo day, a plain market/block-party listing with no music mentioned, and an internal-looking "HOLD" placeholder, while including (with a `flag`) a farmers-market-with-a-named-bluegrass-band, a battle-of-the-bands night, a Guitar Center student showcase, a recurring "BBQ & Bluegrass" night, and one ambiguous "Hootenanny" (name implies folk music, no description to confirm) per the hybrid-event carve-out.
- **Still logged as an UNVERIFIED cross-check source per the task instructions**, not promoted to primary — every one of the 8 venues' own sites were re-confirmed as unusable for this data (thin restaurant-platform stubs with no inline listings, same finding as PR #62), so this API is currently the *only* usable source for these venues, not a secondary confirming an existing primary. Get a few weeks of track record (does it stay in sync with what actually happens at these venues?) before leaning on it without heavy scrutiny.
- **Facebook and Instagram are not reachable at all in this session** — `facebook.com`/`instagram.com` CONNECTs are rejected by the proxy itself (403, policy denial — confirmed via `$HTTPS_PROXY/__agentproxy/status`, not a site-side bot block), distinct from the AXS/Bandsintown pattern documented elsewhere in this file where the *site itself* 403s after a normal tunnel. Would need those hosts added to the egress allowlist to ever try them for these or any other venue. Bandsintown was tried directly for two of these 8 venues (guessed slugs) and hit the same site-side 403 bot-block already documented for Tin Roof/New Realm — not investigated further since the API above already gave real data for all 8 venues.

### charlestonlivemusic.com investigated 2026-09-12 — real site, but doesn't close the Tin Roof gap and doesn't cover the 8 new venues

One-off investigation (network allowlist for `charlestonlivemusic.com`/`*.charlestonlivemusic.com` added same day). **This is a different site from `livemusiccharleston.com` above** — confusingly similar name, unrelated platform: `charlestonlivemusic.com` is ordinary server-rendered WordPress (Divi theme + the "Modern Events Calendar" plugin), not the Dusk/Vite SPA behind `livemusiccharleston.com`. Site is attributed to "SandyMeier.com, LLC, an independent local publication" in its footer.

- **Not a JS shell** — plain `curl` with a browser-like `User-Agent` returns full server-rendered HTML with real event listings inline (confirmed on both `/event-calendar/` and individual venue pages). No same-origin API needed or found.
- **Tin Roof (`/venue/tin-roof/`, confirmed correct slug) currently lists zero events** — the page renders normally (venue description, address, booking notes) but the Modern Events Calendar widget outputs a literal "No event found!" with no pagination/load-more markup hiding further results. This is a real, complete answer from the plugin, not a fetch failure. **Standing Tin Roof gap (own site robots-blocked, Bandsintown bot-blocked) is not closed by this source** — it's simply empty for this venue right now. Worth re-checking occasionally in case the venue starts using this platform's calendar, but don't re-fetch every run; treat as a documented "checked, currently empty" result like the other standing gaps.
- **Site has a `/venues/` directory listing 20 venues total, no pagination** — confirmed by page source (no real "next page" link, only unrelated CSS-class string matches). Of the 11 original tracked venues, pages exist for: Pour House (`the-charleston-pour-house`), Music Farm (`music-farm`), Windjammer (`the-windjammer`), Charleston Music Hall (`charleston-music-hall`), NCC (`north-charleston-coliseum-performing-arts-center`), Firefly Distillery (`firefly-distillery`), plus The Refinery (`the-refinery`, `REF` — already a tracked venue code with metadata in `shows.json` though not in the routine's per-venue fetch table) and Charleston Gaillard Center (`charleston-gaillard-center`, out of scope per the classical-exclusion rule regardless). **No page exists for Credit One Stadium, The Royal American, or New Realm Brewing.**
- **None of the 8 venues added 2026-09-12** (Tattooed Moose ×2, Uptown Social, Tommy Condon's, The Dinghy, Rita's Seaside Grille, Commonhouse Aleworks, Slightly North of Broad) **appear anywhere in the 20-venue list** — no cross-check against `livemusiccharleston.com` was possible for any of them this run.
- **Spot-checked Music Farm's page for data quality** (a venue with its own fully-adequate primary source, so purely a sanity check on this new site, not a data pull): real, current, dated listings through early October 2026 matching the expected pattern — including non-music entries ("Sploinky Rave," "Booty Bounce Charleston – 18+") that would need the standard DJ/dance-party exclusion filter applied if this site is ever used as a source. Confirms the site is generally live and accurate where it has coverage, for whatever that's worth given it covers none of the venues that currently need a new source.
- **Net effect this run: no changes to `shows.json`.** Nothing to add for Tin Roof (source is empty) and nothing to cross-check for the 8 new venues (source has no pages for them). This PR is documentation-only.

### travelerofcharleston.com investigated 2026-09-12 — real per-day snapshot, but not a usable weekly-tracker source

One-off investigation (network allowlist for `travelerofcharleston.com` added same day; found via user search, `travelerofcharleston.com/events/live-music-concerts/`). Ordinary WordPress site (Elementor + the same "Modern Events Calendar" plugin used by `charlestonlivemusic.com`, investigated separately the same day — the two sites are unrelated despite sharing a plugin and don't corroborate or conflict with each other; treat as independent findings).

- **Not a JS shell** — plain `curl` (browser-UA) returns full server-rendered HTML with real event data inline (title, date, time, location, price all present for each `mec-event-article` block on first load).
- **Critical limitation: the page only ever renders "today's" events statically.** All 6 events returned by this fetch (run on 2026-09-12) carried `?occurrence=2026-09-12` in their links and were dated `Sep 12 2026` — none further out. A `<div class="mec-load-more-wrap">` (Modern Events Calendar's standard "load more" control) is present in the markup but **empty** in the raw HTML; further dates only populate via a client-side AJAX call triggered by clicking the button, which a plain fetch never executes. **This makes the site structurally unusable as a source for a weekly, look-ahead tracker** — re-fetching it once a week would only ever show whatever happens to land on the fetch day itself, never the weeks of upcoming shows this routine actually needs.
- **Coverage of tracked venues, from today's 6-event snapshot:** only 2 of 6 tied to a tracked venue — "The Yacht Club on the NÜTRL Beach Stage" at **The Windjammer (`WJ`)** and "Robert Lester Folsom" at **Charleston Pour House (`PH`)**. The other 4 (Drifter Fest, Park Circle Oktoberfest, a culinary "Party with a Purpose," and the already-separately-tracked Low Tide Festival at Riverfront Park/`RFP`) are general city-wide festival/lifestyle listings, not tied to any venue in this routine's table — consistent with the site's own framing as a general Charleston tourism/lifestyle blog ("Live Music & Concerts" is one category tab alongside "Family Friendly," etc.), not a dedicated venue-by-venue live-music calendar.
- **Cross-checked both venue matches against existing `shows.json` ground truth (same date):**
  - Robert Lester Folsom @ Pour House — band name, price ($25), and 6:00 PM time all matched exactly what's already in the data. No new information.
  - The Yacht Club @ Windjammer — band, stage (NÜTRL Beach Stage), and 6:00 PM time matched, but this source showed **$15** where `shows.json` currently has no price for this show (Windjammer's own site doesn't give one either — see "The Yacht Club (Windjammer, Sep 12)" under Standing known issues above). **Not entered into the data** — `travelerofcharleston.com` is neither the venue's own site nor an official ticketing platform, so it doesn't clear the source-priority bar (venue site > official ticketing > nothing), and there's no way from this page alone to tell whether the $15 is corroborated or just carried over from a recurring listing.
- **Zero coverage of Charleston Tin Roof (`TR`, the standing source gap) and zero coverage of any of the 8 venues added 2026-09-12** (Tattooed Moose ×2, Uptown Social, Tommy Condon's, The Dinghy, Rita's Seaside Grille, Commonhouse Aleworks, Slightly North of Broad) in this snapshot — though given the single-day-snapshot limitation above, this isn't a meaningful negative result on its own (a real event at any of these venues today simply wouldn't surface here even if the site covered them in general).
- **No visible "last updated" timestamp**, but every returned event being dated exactly today is itself a strong freshness signal — the underlying calendar plugin is clearly live/current, not a stale cached page.

**Reliability verdict: not reliable enough to use as a routine source, cross-check included.** Not because the data was wrong — everything checked was accurate — but because the only way this routine can fetch it (plain HTTP GET, no JS execution) returns a single day's snapshot with no way to see further out, which is the opposite of what a weekly, look-ahead-by-weeks tracker needs. It also adds nothing where it does have coverage (Windjammer and Pour House already have complete primary sources) and can't meaningfully be evaluated for the two places a new source would actually help (Tin Roof, the 8 new venues) without weeks of daily re-fetching to accumulate a real sample — not a good trade for the effort involved. Not being added to the per-venue fetch strategy table.

**Net effect this run: no changes to `shows.json`.** This PR is documentation-only.

### 2026-09-17 weekly refresh — new findings

- **Firefly Distillery (FD) is now blocked by a CAPTCHA wall, contradicting the "confirmed working" status from the 2026-09-13 run.** Both curl (HTTP 202 → redirect to `/.well-known/sgcaptcha/`) and WebFetch (empty content on the listing page and 3 known event-detail pages) confirm this is a genuine site-side bot-protection change, not a fetch-tool issue. No changes made to FD's 5 existing stored shows this run — all left unverified rather than guessed at. Needs a browser-capable fetch method, or for the block to lift, before this venue is usable again. Treat as a new standing issue going forward (don't re-report every run unless it changes).
- **`beatgig.com` widget discovered behind 4 of the 8 new venues' own sites (DIN, RSG, CHA, SNOB) — blocked by the egress proxy.** Each of these venues' documented-primary pages (`dinghyiop.com/the-music`, `ritasseasidegrille.com/...`, `commonhousealeworks.com/events/`, `snobcharleston.com/...`) return HTTP 200 but contain zero server-rendered schedule content; instead each embeds `<script data-beatgig-venue-slug=... src="https://beatgig.com/iframe/venue-calendar">`. `beatgig.com` itself is rejected by the egress proxy (CONNECT 403). This contradicts the onboarding note's expectation that these 4 venues' own sites (unlike Tattooed Moose) would work as primary — in practice all 4 currently have to fall back to `livemusiccharleston.com` same as Tattooed Moose/Uptown Social/Tommy Condon's. **Worth flagging: `beatgig.com` and `livemusiccharleston.com` use matching venue slugs, and the aggregator's event IDs are prefixed `external_booking_`, suggesting the two may not be independent sources of the same underlying booking data** — not confirmed (beatgig.com itself is unreachable to check), noted rather than assumed. If `beatgig.com` is ever added to the egress allowlist, worth revisiting whether it's actually a distinct, more-primary source or just a mirror of the same data already reachable via `livemusiccharleston.com`.
- **Tattooed Moose's new `/events` page (both TMPC and TMJI) is live but stale — showing only 2025 dates (plus a single Jan 1 2026 entry), not current fall/winter 2026 data.** A first-pass WebFetch summary silently dropped the year and made this look like current data with several spurious apparent conflicts — caught and discarded on a careful verbatim re-fetch. **If this page is ever checked again, verify the year explicitly before trusting any diff it produces** — the failure mode is quiet and easy to miss. `livemusiccharleston.com` remains the only usable current source for both locations; TMPC's aggregator depth is thin (only 4 forward-looking events, through 2026-09-27).
- **Music Farm: the existing "NEW DATE: Drew Sidora" show (2026-10-17) has disappeared from `musicfarm.com/calendar/` entirely**, with that slot now showing a non-music "House Party" DJ event instead. Couldn't verify further — both `axs.com/venues/124602` (403) and `ticketmaster.com` (egress-blocked) were unreachable this run. Left unchanged in `shows.json` rather than guessed at (this diff format has no safe way to represent "possibly cancelled"); worth a human check before the next run touches this slot.
- **Windjammer: `Macelyn Batten` (2026-09-18, Inside Stage, 9:30 PM) has disappeared from the venue's site, with the identical date/stage/time slot now showing `FlashMob` instead.** Recorded `FlashMob` as a new show rather than a replacement (couldn't confirm whether this is a straight rebooking or an unrelated addition) — `Macelyn Batten` left in stored data pending manual confirmation.
- **Data-model limitation confirmed for real this run, not hypothetical:** Pour House's "The Headstrings Collective – Monday Muse" has a genuine separate Deck Stage set (6:00 PM) on 2026-09-28 in addition to the already-stored Main Stage set (6:30 PM) that same date — same band, same venue, same date, different stage/time. The `(venue, date, band)` uniqueness rule this routine validates against has no way to represent this as two rows, so the Deck Stage set could not be added. Left out of this run's diff entirely rather than silently overwriting or dropping the existing Main Stage entry — flagging as an open product question (schema currently can't model same-band/same-day/different-stage doubleheaders at Pour House) rather than something this run could safely resolve.
- **AXS bot-block confirmed unchanged (not re-litigated in depth): still 403 at CMH, both NCC halls, WJ, C1S, and MF this run** — consistent with the standing documented block, tested lightly (1 request each) rather than repeatedly retried, per standing guidance.
- **`ticketmaster.com` itself (not just AXS) was blocked at the network egress-proxy level this run** for direct event-page fetches — distinct from the documented AXS/price bot-block. Didn't block data collection (venue sites and previously-stored Ticketmaster URLs covered what was needed) but worth knowing if a future run needs to fetch a fresh Ticketmaster page directly.
- **Two low-confidence single-field corrections applied and flagged rather than silently trusted:** Tommy Condon's 2026-10-18 8:00 PM slot ("Kevin Church" → "Chris Tidestrom") and The Dinghy's 2026-11-14 7:00 PM slot ("Eddie Bush" → "HALF BAD") both came solely from a `livemusiccharleston.com` re-fetch with no independent venue-site data to arbitrate — applied as corrections (the aggregator is currently the *only* source for these two venues) but each show now carries a `flag` noting the swap is unverified.

### beatgig.com investigated 2026-09-17 — confirmed same underlying data as livemusiccharleston.com, not independent

One-off investigation (network allowlist for `beatgig.com` and `backend.beatgig.com` added same day by the user). Resolves the "possible non-independence" flag raised in the 2026-09-17 weekly-refresh PR (#78) for CHA/SNOB.

- **`beatgig.com` itself is a Next.js frontend; the real API lives at `backend.beatgig.com/api/v1/graphql`** (found by fetching `beatgig.com`'s own JS bundles and grepping for `beatgig.com` URLs — same technique as the `livemusiccharleston.com` investigation). Undocumented, reverse-engineered from bundle internals, not an official public API — treat as fragile and liable to change without notice.
- **Working (unauthenticated, read-only) query, found by trial and error against the live schema** (the bundle's own query text used a `venue(slug: ...)` root field that no longer exists on the live schema — schema had drifted from what's in the shipped bundle):
  ```graphql
  query T($start: DateTime!, $offset: Int!, $limit: Int!, $slugs: [String!]!) {
    calendarBookings(start: $start, limit: $limit, offset: $offset, organizationSlugs: $slugs) {
      canFetchMore
      bookings: calendarBookings {
        bandConfiguration artistCategory publicEventDescription artistName
        venueSlug venueName startTime bookingType id
      }
    }
  }
  ```
  `organizationSlugs` takes the same venue slug embedded in the venue's own site (`data-beatgig-venue-slug="..."`, visible in the page source of any venue using the widget). Paginate via `offset`/`limit` while `canFetchMore` is true. `startTime` is UTC.
- **Confirmed identical to `livemusiccharleston.com`'s data for both CHA and SNOB** — same event counts (37 for CHA, 7 for SNOB), same date ranges, and the handful of "extra" BeatGig events not yet in `shows.json` are the exact same non-music items (Green Market Night, Trivia, DJ nights, National Drink a Beer/Lager Day, block-party listings) the earlier fetch already correctly excluded. `bookingType` distinguishes `internal` (booked directly through BeatGig) from `external` (synced in from elsewhere) — the `external_booking_` ID prefix matches `livemusiccharleston.com`'s own event IDs exactly, confirming these two sites share the same backend rather than being independently-curated sources.
- **Net effect: no changes to `shows.json`** — CHA and SNOB were already fully in sync via `livemusiccharleston.com`. Worth keeping this query documented anyway: it's one hop closer to the actual source (the venue's own embedded widget, not a third-party aggregator site), so if `livemusiccharleston.com` ever goes stale or dark, this is a ready-made fallback for any venue using a BeatGig calendar widget (confirmed so far: CHA, SNOB, and the now-untracked DIN/RSG).

### Digest email overhaul, 2026-09-17 (interactive session, several PRs)

- **Branding**: every `"Lowcountry Show Tracker"` string in `worker.js` was leftover from before the 2026-09-11 rename — the digest's own title banner (most visible), the unsubscribe flow's confirmation page/text, and every transactional email's fallback from-address/subject. All fixed to "Show Notice."
- **Formatting to match the site**: digest title font switched from Georgia serif to Bebas Neue (loaded the same way `index.html` loads it, condensed-sans fallback for clients that strip external fonts); subtitle replaced with the site's actual current tagline instead of stale copy.
- **Section order**: "Added This Week" now renders before "My Shows" (was the reverse), per explicit user request.
- **Per-card date/time legibility**: the per-card date text was originally 11px — smaller than every other line on the card. Removed entirely in favor of grouping: each section (My Shows/Added This Week/Everything Else) now groups its shows into one day-head bar per date, mirroring the site's own `groupByDayHTML()` (venue-then-time sort within a day), instead of repeating the date on every card. Show/doors times were also wrapping mid-value ("9:30" / "PM" on separate lines) in the narrow 64px time-block column — fixed by splitting the AM/PM suffix into its own smaller inline span (same trick the site's `formatTimeValue()` uses) and switching that block to Bebas Neue too.
- **`verifyResendWebhookSignature()` crash, found via live Workers Observability logs**: called `atob()` on the webhook secret with no error handling — a malformed/edge-case secret computation threw uncaught, which the outer `fetch()` handler turned into a raw 500, and Resend retried the failing delivery repeatedly. Because the throw happened before the delivered/opened/clicked counter-increment code ran, digest engagement stats were silently undercounted for every event this hit. Fixed: wrap the signature computation in try/catch, fail closed (same as an actual bad signature — no security weakening), log the real cause.
- **"Added This Week" backdating, 2026-09-17: a one-time cleanup, not a standing policy.** Same pattern as PR #77 (2026-09-13): after a data-heavy routine run adds/updates many shows on the same day, most of them share `added` = that day's date and all pile into the "Added This Week" section at once (60 shows did, 2026-09-17, after the weekly refresh). Backdated every qualifying show's `added` field to `2026-09-01` except Eric Lindell (kept at its real `added: 2026-09-13`) purely to declutter that section as a manual cleanup pass. **Explicit user correction, same day: this is NOT a standing/automatic practice going forward.** New shows should keep their real, genuine `added` date from here on — don't pre-emptively backdate every future addition just because it might pile up the section again. If the section gets cluttered again later, that's a deliberate decision to make in the moment (like this one), not something to bake into the routine's automated behavior.

### The Wednesday-3pm-digest mystery, 2026-09-17 — root cause never actually found, but ruled out what it wasn't

A subscriber-facing digest fired Wednesday 2026-09-16 at exactly 3:00 PM ET — the *exact* old pre-2026-09-12 schedule, both day and time, to the exact expected subscriber list. Investigated at length:
- **Not a stale/duplicate Cloudflare Cron Trigger.** Both deploys since the Thu-1pm-ET schedule change (`wrangler deploy`'s own logged output for PR #67 *and* PR #68, the last deploy in that day's sequence) explicitly printed `schedule: 0 17 * * thu` as the trigger Cloudflare's API accepted. The live dashboard's Triggers panel agreed (Thursday, count of 1).
- **But it did fire via the real Cron Trigger, not a manual/external call.** Workers Observability logs (the per-Worker "Logs" tab, not the bindings/architecture Overview page) showed a `show-tracker-api scheduled OK` entry at exactly 2026-09-16 15:00:36 EDT — `scheduled` is the log type Cloudflare only emits for an actual Cron Trigger invocation calling the `scheduled()` handler; nothing else in this codebase reaches that code path.
- **Net conclusion: genuinely contradictory evidence, never resolved.** Every config source (repo, both deploy logs, live dashboard) agrees the trigger is Thursday 1pm ET; the actual invocation log says it fired Wednesday 3pm ET via the real Cron Trigger path. Best working theory is a stuck/stale entry on Cloudflare's own backend cron dispatcher that the API/dashboard don't reflect — a real (if uncommon) class of platform bug, not something wrong in this repo. A manual redeploy was triggered the same day (via `workflow_dispatch`) as a "nudge" attempt; whether it actually fixed anything wasn't confirmed by the end of the session. **Worth checking**: did `scheduled OK` fire again on Wednesday 2026-09-23, or only correctly on Thursday? That would settle whether the nudge worked.
- Incidentally surfaced two Resend-webhook `error` log entries right after the Wednesday send — that's what led to finding and fixing the `verifyResendWebhookSignature()` crash above.
- **Recurred 2026-09-24 — same failure class, different time.** Subscribers received two identical weekly digests (7:07am and the correct 1pm ET). The root cause of *why* Cloudflare's dispatcher occasionally fires an extra invocation of the real `scheduled()` handler is still unresolved — same open mystery as above — but rather than keep waiting on it, `sendDigestIfNotAlreadySentToday()` now guards the cron path with a `digest-sent:<date>` KV marker (2-day TTL) set *before* the send starts, so a second same-day fire from any cause is a safe no-op. The owner-only `/api/test-send-digest-now` endpoint intentionally bypasses this guard — it's meant to force a real send on demand, including on a day the scheduled digest already went out.
- **Guard tightened 2026-09-24 (later same day).** The same-UTC-date key above could not have stopped the 2026-09-16 Wednesday extra fire (different date), and a phantom that fires *before* the real slot would have sent first and the real send would then be the one skipped. Noticed along the way: the 2026-09-16 fire (19:00:36 UTC Wed) matches the *old* `0 19 * * wed` schedule to the minute, which points to a stale old schedule entry on Cloudflare's side rather than a random fire (this is an inference; it isn't confirmed). `scheduled()` now calls `sendDigestIfDue()`, which (1) sends only when the event's `scheduledTime` matches `DIGEST_CRON` (`0 17 * * thu`, a constant the deploy workflow checks against `wrangler.toml`), (2) allows at most one send per 6 days (`digest-last-sent` KV key), and (3) logs `event.cron` + `scheduledTime` on every fire, so the next off-schedule fire shows in Workers Logs exactly which schedule Cloudflare thinks it's running. Side effect: the dashboard's manual cron "Trigger" test won't send unless it's run at the real slot. Use `/api/test-send-digest-now` for that; it still bypasses both guards.

### Mobile/layout polish pass, 2026-09-17 (PRs #85–#89)

A run of small user-reported visual fixes, each shipped as its own PR and merged immediately per explicit ask:
- **Page margins + jump-link alignment (PR #85).** `.wrap` max-width 880px → 760px, and `.section-title-row` capped at 480px to match `.card`'s max-width — the per-section jump-link buttons (e.g. "My Shows ↓") were sitting flush with the much-wider `.wrap` edge, far to the right of the actual (narrower) show cards below them. Also folded into this PR: removed two Pour House entries (9/19 Pop-Up Market, 10/17 The Poho Night Market) that were vendor/market events, not live music — user-flagged, despite both having an existing `flag` noting incidental live music. **Vendor/market events are excluded outright even with a live-music component** — this sharpens the existing "non-music events — exclude confidently" rule above, which previously treated an explicit live-music mention in a market/vendor event as a reason to include-and-flag rather than exclude.
- **"Sign out" wrapping, two rounds (PRs #86–#87).** A longer signed-in email address could push "Sign out" onto its own line, separated from its "·" separator (fixed with a non-breaking space, PR #86) — but at real-world email lengths (~23+ chars) the two words of "Sign out" itself could still split across lines (fixed with `white-space:nowrap` on `.signin-action`, PR #87). Two separate wrap points needed two separate fixes.
- **"All Shows" row fit on mobile (PR #88).** The venue-filter dropdown already sat beside the "All Shows" heading on mobile, but "Back to top" didn't fit alongside and wrapped to its own line. Fixed by narrowing `.venue-filter-toggle` to 112px (from 168px) on mobile only, plus trimming the pill buttons' padding and the row gap slightly — tight but fits at 375px/390px/414px; a very old ~360px-wide phone can still wrap, judged an acceptable tradeoff rather than shrinking further and hurting legibility.
- **Simplified signed-in control (PR #89).** Dropped "Signed in as `<email>`" entirely, leaving just "Sign out" — showing the email read as too busy right next to My Shows, one of the most-viewed parts of the page. The email isn't needed to act on the control.

### Digest engagement showing zero opens investigated, 2026-09-17 (not a bug — client-side)

User reported the admin panel's digest engagement stats showed zero opens despite genuinely opening that week's email, and asked whether their VPN + Safari Private Browsing use could be the cause. Investigation:
- **VPN/Private Browsing are unlikely culprits.** A VPN only changes IP, doesn't block image loads. Safari's tracking prevention targets cookie-based cross-site tracking, not a plain `<img>` GET. Apple Mail's "Protect Mail Activity" (Mail Privacy Protection) — a different, Mail-app-specific feature — actually *inflates* opens toward 100% via proxy pre-fetching, the opposite of suppressing them.
- **User checked Resend's own per-email Insights panel directly** (screenshot) for the specific digest send: the delivery timeline stopped at "Delivered," no "Opened" step at all, while "Use custom subdomain for open/click tracking" both showed green/"Doing great" — confirming tracking *is* enabled and properly configured at the account level. This ruled out both a Resend-config problem and a webhook problem: Resend itself never saw the tracking pixel requested, so our webhook was never going to fire regardless of its own health.
- **Conclusion: the reader's mail client never loaded the remote tracking-pixel image** for that specific open — most likely "load remote images" wasn't enabled/triggered in whatever client was used. Not fixable from this codebase.
- **Added general-purpose webhook health diagnostics anyway (PR #90)**, since the two failure modes ("nothing's arriving" vs. "everything's being rejected") otherwise look identical (both show all-zero counters) and this was worth being able to rule out quickly next time without a per-email Resend dashboard lookup. `handleResendWebhook` now records the most recent webhook call of *any* type (timestamp, event type, signature status) to a single KV key, plus a per-day signature-failure counter (reuses `digestStatsWindow`'s `digest-stats:{type}:{date}` bucketing under `type: 'sigfail'`). Admin panel shows it in red when something looks wrong: secret not configured, no call ever received, recent signature failures, or nothing in over ~10 days (longer than the weekly cadence). This cannot see or explain an individual reader's pixel-loading behavior — that stays Resend-Insights-only, per-email.

### Background research-routine agents silently cancelled mid-run, 2026-09-17

The weekly `shows.json` refresh dispatched 11 parallel per-venue fetch agents (one each for Pour House, Music Farm, Royal American, Windjammer, Credit One Stadium, NCC, Charleston Music Hall, Tin Roof, The Refinery, a combined US/CHA/SNOB livemusiccharleston.com batch, and a combined FD/NRB/GC/CJF/RFP quick-checks batch). Only 2 of 11 (Credit One Stadium, the quick-checks batch) ever reported back; the other 9 went silent for roughly an hour with no completion notification. Asked each directly via `SendMessage` to its agent ID for a status update — every one of the 9 came back `"Agent ... was stopped by the user and won't be resumed."` None had actually hung or been stuck retrying; they'd been cancelled outright, sometime before ever producing output, with no notification of the cancellation reaching the orchestrating session at the time it happened. **Lesson for future sessions:** a background agent's silence past a reasonable duration isn't evidence it's still working — `SendMessage` to its agent ID is a cheap, direct way to confirm whether it's actually still alive versus quietly cancelled, rather than continuing to wait indefinitely on a notification that will now never come. All 9 were relaunched from scratch (fresh per-venue diffs re-pulled from the current `shows.json`, since two Pour House entries had been removed in the interim) rather than assumed resumable.

### charlestonlivemusic.com timezone-mislabeling bug found and fixed, 2026-09-17 (post-PR #91)

User caught two Refinery times as wrong after PR #91 merged: Dani Rucker (stored `sh: 4:00 PM`, actually doors 7:00 PM / show 8:00 PM) and Trampled by Turtles (stored `dr: 3:30 PM` — a "doors time" PR #91 itself had invented — actually doors 6:00 PM, show correctly already 7:30 PM). Root-caused by fetching the raw JSON-LD directly (`curl` + regex on the actual event pages, not WebFetch's markdown conversion, which the PR #91 research pass had already noted "hallucinates" times from boilerplate text):

- **The actual bug:** charlestonlivemusic.com's schema.org `startDate`/`endDate` fields are the venue's real Eastern wall-clock time, but the site's own schema generator tags them with a literal `+00:00` (UTC) suffix instead of the correct `-04:00`/`-05:00` offset — e.g. Trampled by Turtles' real 7:30 PM show is emitted as `"startDate":"2026-10-16T19:30:00+00:00"`. Converting that "UTC" value to Eastern (correctly, by our own conversion logic — subtracting 4 hours for EDT) takes an already-correct local time and shifts it 4 hours too early, producing a plausible-looking but fabricated time. Confirmed the same pattern on Ashley McBryde and Judah & The Lion (both `19:30:00+00:00` → real 7:30 PM show, not a `dr` value).
- **A second, separate problem on the same source:** several Refinery "Limbo" showcase event pages (Dani Rucker, Caleb Lee Hutchinson, Jesse Roper) return `"startDate":"1970-01-01T00:00:00+00:00"` — the Unix epoch, i.e. no real date was ever entered in the site's CMS for these events — not a timezone artifact at all, just a placeholder with the shape of a real timestamp. PR #91's research pass (and, it turns out, an earlier 2026-09-01 session for Dani Rucker) treated these as legitimate data and, worse, used one bad value (Dani Rucker's fabricated 4:00 PM) as a "confirmed" calibration point to justify inferring a fake "doors is always ~4 hours before show" rule, which is how the incorrect Trampled by Turtles/Ashley McBryde/Judah & The Lion/Beartooth `dr` values got introduced in the first place. **A stored value is not ground truth just because an earlier session recorded it with confidence** — re-derive from the primary source when validating a new inference, don't chain trust through a value nobody has re-verified.
- **Checked whether this is systemic:** verified Tin Roof's tockify ICS feed (`DTSTART:20260902T230000Z` for Sheer Terror, converts correctly to the doors time — 7:00 PM — stated in the feed's own `SUMMARY` text) and livemusiccharleston.com's `/api/events/global` (`"start":"2026-09-20T21:00:00Z"` for a Uptown Social FlashMob show, converts correctly to the already-confirmed 5:00 PM). Both are genuinely, correctly-labeled UTC — **this bug is isolated to charlestonlivemusic.com specifically**, not a problem with this routine's UTC-conversion logic in general.
- **Fixed:** Dani Rucker (`sh: 8:00 PM`, `dr: 7:00 PM`, per user), Trampled by Turtles (`dr: 6:00 PM`, per user). Reverted the three other fabricated `dr` values from PR #91 (Ashley McBryde, Beartooth back to `"see ticket link"`; Judah & The Lion's `dr` reverted but its `sh` genuinely gained `7:30 PM`, since that reading — literal local time, ignoring the false UTC tag — matches the validated McBryde/Trampled-by-Turtles pattern). Reverted Jed Harrelson and Jesse Roper's `sh` fills from PR #91 back to `"see ticket link"` (both were copied from the debunked Dani Rucker/Caleb Lee Hutchinson calibration, not independently sourced). Left Caleb Lee Hutchinson's pre-existing `sh: 3:00 PM` value as-is (out of scope, predates this session) but added a `flag` — same epoch-zero JSON-LD, same suspicion, needs independent confirmation, not silently corrected without one.
- **Corrected routine instruction for The Refinery's fetch strategy, replacing the "use charlestonlivemusic.com JSON-LD `startDate` for times" guidance:** before trusting any `startDate`/`endDate` value from this source, fetch the raw JSON-LD directly (curl, not WebFetch) and check it isn't `1970-01-01T00:00:00` (placeholder/no real date — if so, treat the event as having no time data from this source at all, leave `sh`/`dr` as `"see ticket link"`). If it's a real date, read the date/time digits literally as America/New_York local time — **do not** apply a UTC→Eastern conversion to it, despite the `+00:00` suffix, since that suffix is wrong on this site. Never infer a "doors time" from this field; a real (non-epoch) value here has only ever been observed to equal the actual show start time, never doors — if doors time is needed and not stated elsewhere, leave it unconfirmed rather than guess an offset. And never use another session's previously-stored value as independent confirmation of a new inference technique — re-derive from the primary source.

## Repo notes

- `cowork-task-instructions.md` was rewritten from scratch in an earlier session.
- **Branch hygiene is now clean.** "Automatically delete head branches" is enabled and confirmed working (every PR from #18 on had its branch auto-deleted on merge). The 13 legacy branches that predated the setting (`digest-email-header-border`, `fav-storage-race-fix`, `favorite-match-openers`, `header-size-tweak`, `notes-and-festival-data`, `project-notes-consolidated`, `project-notes-update-2`, `rfp-venue-url-wrap`, `riverfront-revival-venue-fix`, `star-favorite-prompt`, `star-modal-and-columns`, `utility-row-alignment-fix`, `utility-row-jump-links`) were deleted manually as of 2026-09-10 — `main` is now the only branch in the repo. (Note for any future automated session: git push credentials here get an HTTP 403 specifically on branch **deletion**, confirmed 2026-09-10 — a deliberate scope restriction, not a bug, and no GitHub MCP tool exists for it either — so an automated session still can't do this cleanup itself if it's ever needed again; it has to be done manually.)
- **A single long remote session (2026-09-11) reused one branch name, `claude/kind-brahmagupta-wmw4ya`, across ~26 sequential PRs (#26–#51)** — each PR merged, then the same branch name was recreated from the new `main` tip for the next round of work, rather than a fresh branch per change. This is a property of that session's designated-branch setup, not a hygiene regression; branch auto-delete-on-merge still applies normally between rounds. A future session picking up work here should still create/reuse its own designated branch per its own instructions rather than assuming a fixed name.
